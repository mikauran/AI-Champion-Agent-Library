---
phase: 07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/routes/api/tryitout/jobs/+server.ts
  - src/routes/api/tryitout/jobs/[id]/+server.ts
  - src/routes/api/tryitout/jobs/[id]/artifact/+server.ts
  - src/routes/api/tryitout/jobs/jobs.test.ts
  - src/lib/tryItOut.ts
  - src/lib/tryItOut.test.ts
  - src/lib/testing/fakeJobApi.ts
  - src/lib/components/TryItOutPanel.test.ts
autonomous: true
requirements: [SC-01, SC-02, SC-04, SC-05, SC-06, SC-07]

must_haves:
  truths:
    - "SC-01: `POST /api/tryitout/jobs` returns `{ jobId, status: 'queued' }` immediately — it does not await the model call, and no container or `pi` process is started"
    - "SC-02: posting a multipart form with a file writes that file's bytes under `.tryitout-work/<jobId>/input/` before the response is returned"
    - "SC-06: `GET /api/tryitout/jobs/:id` returns a `JobUpdate` shaped exactly `{ status, events, error }`; `GET /api/tryitout/jobs/:id/artifact` returns the real generated file with an attachment `Content-Disposition` when status is `succeeded`, and refuses with 409 otherwise"
    - "SC-06 (unknown job): both GET routes return 404 for an id that is not in the store, and for a malformed id — never a 500 and never a filesystem error"
    - "SC-07: `src/lib/tryItOut.ts` reaches the backend via `fetch` only; its three exported signatures and the four exported types are byte-identical to the frozen contract in docs/job-api-contract.md"
    - "SC-07 (UI untouched): `src/lib/components/TryItOutPanel.svelte` and `src/routes/agents/[slug]/+page.svelte` have zero source changes in this plan, and the panel's existing transport-discipline test (no `fetch(`, no `EventSource`, no URL literal) still passes"
    - "SC-04 end-to-end: driving POST then polling GET through the real handlers with a mocked OpenAI client observes status reaching `succeeded` and the artifact route serving the generated text"
    - "SC-05: the events delivered to `subscribeProgress`'s callback during a real run are all `type: 'info'`"
    - "An oversized upload (>200 KB) is rejected with 413 before anything is written to disk or sent to the model"
    - "Concurrency: two POSTs issued back-to-back produce two distinct jobIds and two independently-pollable statuses; neither job's events appear in the other's `JobUpdate`"
  artifacts:
    - path: "src/routes/api/tryitout/jobs/+server.ts"
      provides: "POST job creation, multipart parse, size cap, input write, fire-and-forget run"
      exports: ["POST"]
    - path: "src/routes/api/tryitout/jobs/[id]/+server.ts"
      provides: "Job status polling returning JobUpdate"
      exports: ["GET"]
    - path: "src/routes/api/tryitout/jobs/[id]/artifact/+server.ts"
      provides: "Artifact download, succeeded-only"
      exports: ["GET"]
    - path: "src/lib/tryItOut.ts"
      provides: "Real fetch-backed client, frozen signatures preserved"
      exports: ["JobStatus", "JobEvent", "JobUpdate", "SubmitResult", "submitJob", "subscribeProgress", "downloadArtifact"]
    - path: "src/lib/testing/fakeJobApi.ts"
      provides: "Shared fetch stub emulating the three routes, for client and component tests"
      exports: ["installFakeJobApi"]
  key_links:
    - from: "src/lib/tryItOut.ts"
      to: "/api/tryitout/jobs"
      via: "fetch POST with FormData"
      pattern: "fetch\\('/api/tryitout/jobs'"
    - from: "src/lib/tryItOut.ts"
      to: "/api/tryitout/jobs/:id"
      via: "poll loop in subscribeProgress"
      pattern: "api/tryitout/jobs/\\$\\{jobId\\}"
    - from: "src/routes/api/tryitout/jobs/+server.ts"
      to: "src/lib/server/tryItOutRunner.ts"
      via: "fire-and-forget runJob(jobId).catch(...)"
      pattern: "runJob\\("
    - from: "src/routes/api/tryitout/jobs/[id]/artifact/+server.ts"
      to: ".tryitout-work/<jobId>/output/result.txt"
      via: "readFile(outputFilePath(id))"
      pattern: "Content-Disposition"
---

<objective>
Close the tracer loop: expose the three HTTP routes over plan 07-03's engine, and replace the mock bodies in `src/lib/tryItOut.ts` with real `fetch()` calls — without changing a single line of UI.

Purpose: SC-06 and SC-07 are the swap itself. The frozen contract in `docs/job-api-contract.md` promised that going mock -> real touches only `src/lib/tryItOut.ts`; this plan is where that promise is either kept or broken. The existing Phase 6 tests exercise the real (mock-backed) client through the panel, so they must be migrated to a fetch stub in the same change — otherwise the suite goes red for reasons unrelated to correctness.

Output: three `+server.ts` handlers, a rewired client, a shared `installFakeJobApi` test stub, and migrated client/panel tests.
</objective>

<execution_context>
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/workflows/execute-plan.md
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-CONTEXT.md
@.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-RESEARCH.md
@.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-PATTERNS.md
@.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-03-SUMMARY.md
@docs/job-api-contract.md
@src/lib/tryItOut.ts
@src/lib/components/TryItOutPanel.svelte
</context>

<interfaces>
<!-- Everything the executor needs. Do not go exploring the codebase for these. -->

FROZEN client API from `docs/job-api-contract.md` — signatures must not change:
```typescript
submitJob(agentId: string, task: string, file?: File | null): Promise<SubmitResult>
subscribeProgress(jobId: string, onUpdate: (u: JobUpdate) => void): () => void  // returns unsubscribe
downloadArtifact(jobId: string): Promise<void>
```

FROZEN region of `src/lib/tryItOut.ts` — lines 1-25 (the header comment plus the four type exports) are preserved verbatim. Lines 27-49 keep their exact signatures; only the bodies change. Everything from line 51 (the `MOCK IMPLEMENTATION — DELETE WHOLESALE` banner) to end of file is deleted.

Two helpers from the deleted mock region that must be RE-CREATED in the new implementation (they are not mock-specific):
```typescript
function triggerTextDownload(...)   // lines 147-156: Blob -> createObjectURL -> anchor.click() -> remove() -> revokeObjectURL
                                    // The revokeObjectURL call is threat T3 from Phase 6 — do not drop it.
function safeFileToken(id: string)  // lines 158-160: id.replace(/[^a-zA-Z0-9._-]/g, '_') — used for the download filename
```

Server engine API from plan 07-03 (`src/lib/server/`):
```typescript
// tryItOutJobs.ts
export interface JobRecord { jobId, agentId, task, fileName, status, events, error, createdAt }
export function createJob(agentId: string, task: string, file: File | null): JobRecord
export function getJob(jobId: string): JobRecord | undefined
export function isValidJobId(id: unknown): id is string
export function inputDir(jobId: string): string
export function inputFilePath(jobId: string): string    // ALWAYS .../input/input.txt — never the client filename
export function outputFilePath(jobId: string): string
// tryItOutRunner.ts
export function runJob(jobId: string): Promise<void>    // never rejects; records its own failed status
```

SvelteKit route conventions (verified live in RESEARCH.md against svelte.dev/docs/kit/routing):
```typescript
import { json, error } from '@sveltejs/kit'
import type { RequestHandler } from './$types'
export const POST: RequestHandler = async ({ request }) => { const form = await request.formData(); ... }
export const GET:  RequestHandler = async ({ params }) => { params.id /* dynamic [id] segment */ }
```

Phase 6 guard test that MUST keep passing unchanged (`src/lib/components/TryItOutPanel.test.ts` lines 106-116):
```typescript
const src = readFileSync('src/lib/components/TryItOutPanel.svelte', 'utf8')
expect(src).toMatch(/from '\$lib\/tryItOut'/)
expect(src).not.toMatch(/fetch\s*\(/)
expect(src).not.toMatch(/EventSource/)
expect(src).not.toMatch(/XMLHttpRequest/)
expect(src).not.toMatch(/https?:\/\//)
expect(src).not.toMatch(/\{@html/)
```
</interfaces>

<artifacts_this_phase_produces>
| Artifact | Symbols | Plan |
|---|---|---|
| `src/lib/server/tryItOutModel.ts` | `MODEL`, `TEMPERATURE`, `modelParams()` | 07-01 |
| `scripts/probe-openai-model.ts` | (script) | 07-01 |
| `data/agents/demo-rfi-triage.yaml` | agent slug `demo-rfi-triage` | 07-02 |
| `data/tryitout-prompts/demo-rfi-triage/skill.md` | (prompt content) | 07-02 |
| `data/tryitout-prompts/demo-rfi-triage/sample-rfi.txt` | (demo input fixture) | 07-02 |
| `scripts/set-try-it-out-mode.ts` | `setTryItOutMode()` | 07-02 |
| `src/lib/server/tryItOutJobs.ts` | `JobRecord`, `createJob`, `getJob`, `pushEvent`, `setStatus`, `workDir`, `inputDir`, `outputDir`, `inputFilePath`, `outputFilePath`, `isValidJobId` | 07-03 |
| `src/lib/server/tryItOutPrompts.ts` | `AgentIdSchema`, `loadPromptFor`, `composePrompt` | 07-03 |
| `src/lib/server/tryItOutRunner.ts` | `runJob` | 07-03 |
| `src/routes/api/tryitout/jobs/+server.ts` | `POST` | **07-04** |
| `src/routes/api/tryitout/jobs/[id]/+server.ts` | `GET` | **07-04** |
| `src/routes/api/tryitout/jobs/[id]/artifact/+server.ts` | `GET` | **07-04** |
| `src/lib/tryItOut.ts` | `submitJob`, `subscribeProgress`, `downloadArtifact` (bodies rewired) | **07-04** |
| `src/lib/testing/fakeJobApi.ts` | `installFakeJobApi` | **07-04** |
| `src/lib/tryItOutSession.ts` | `readJobIdFromUrl`, `writeJobIdToUrl`, `clearJobIdFromUrl` | 07-05 |
</artifacts_this_phase_produces>

<threat_model>
ASVS Level 1. Block on: high severity.

| # | Threat | Severity | Mitigation (implemented and verified in this plan) |
|---|---|---|---|
| T4-1 | **Path traversal via `params.id`** on the two GET routes — `params.id` flows into `outputFilePath()`. | **high** | Both handlers call `isValidJobId(params.id)` (UUID regex, plan 07-03) and return 404 before any path helper runs; the path helpers throw on invalid ids as defense in depth. `getJob` requires an exact `Map` hit, so only server-minted UUIDs resolve. Verified by tests posting `..%2F..%2Fetc%2Fpasswd` and `../etc` as the id and asserting 404 with no filesystem access. |
| T4-2 | **Path traversal via the uploaded filename.** | **high** | The POST handler writes to `inputFilePath(jobId)` — a fixed server-chosen path. `File.name` is stored on the record for display only and never passed to `join()`. Verified by a test uploading a file named `../../escape.txt` and asserting the bytes land at `input/input.txt`. |
| T4-3 | **Unauthenticated unbounded upload** to a public demo endpoint — disk exhaustion or a runaway model bill. | **high** | A hard `MAX_UPLOAD_BYTES = 200 * 1024` cap (RESEARCH.md Open Questions §3) is checked against `file.size` AND against `task.length` BEFORE `mkdir`/`writeFile` and before `runJob` is scheduled; over-limit requests get 413 and create no job. Verified by a test asserting a 201 KB upload yields 413 and no new store entry and no file on disk. Residual risk (no rate limiting, no auth) is accepted: this is an internal demo per the contract's "no auth beyond a shared-token placeholder" scope statement, and the endpoint costs one capped model call per request. |
| T4-4 | **API key reaching the client (SC-09).** | **high** | Routes import only from `src/lib/server/**`; `src/lib/tryItOut.ts` (a client module) imports nothing from `src/lib/server/`. Verified by a grep asserting `src/lib/tryItOut.ts` contains no `$lib/server` or `lib/server` import and no `OPENAI_API_KEY`. |
| T4-5 | **Artifact served for a non-succeeded job**, leaking a partial or stale file, or a 500 from a missing file. | medium | The artifact handler returns 409 unless `status === 'succeeded'`, and returns 404 if the output file is missing despite a succeeded status. Verified by tests for both branches. |
| T4-6 | **Content-Disposition header injection** via the filename. | medium | The download filename is built as `aic-job-${jobId}.txt` from a validated UUID only — no user-controlled substring reaches the header. |
| T4-7 | **XSS via model output rendered in the feed / error block.** | low | Unchanged from Phase 6: the panel renders `{event.summary}` and `{error}` as text with no `{@html}`, guarded by the existing transport-discipline test. Artifact text is downloaded as `text/plain`, never injected into the DOM. |
| T4-8 | **Object-URL leak** in the real `downloadArtifact` (Phase 6 threat T3). | low | `URL.revokeObjectURL(url)` is retained in the rewritten implementation and asserted by the existing `tryItOut.test.ts` download test. |

No unmitigated high-severity threats.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The three SvelteKit route handlers</name>
  <files>src/routes/api/tryitout/jobs/+server.ts, src/routes/api/tryitout/jobs/[id]/+server.ts, src/routes/api/tryitout/jobs/[id]/artifact/+server.ts, src/routes/api/tryitout/jobs/jobs.test.ts</files>
  <read_first>
    - `.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-03-SUMMARY.md` (the exact exported API of the three server modules)
    - `src/lib/server/tryItOutJobs.ts` and `src/lib/server/tryItOutRunner.ts` (current source — the functions being wired)
    - `.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-RESEARCH.md` Architecture Patterns -> Pattern 1 (all three handler skeletons, verified against SvelteKit routing docs) and Anti-Patterns (never await the LLM call in POST) and Pitfall 3 (empty-File semantics)
    - `src/routes/agents/[slug]/+page.server.ts` (the repo's existing `error(404, ...)` usage and plain-object response shaping)
    - `docs/job-api-contract.md` (the endpoint shapes these implement)
  </read_first>
  <behavior>
    Calling the exported handler functions directly with a constructed request object (no HTTP server needed), with `openai` mocked:
    - `POST` with valid `agentId` + `task` + a text file: resolves to a JSON body `{ jobId, status: 'queued' }` where `jobId` is a UUID; the file's bytes exist at `inputFilePath(jobId)`; the response returns without waiting for the model call.
    - `POST` with `agentId`/`task` missing or non-string: 400, no job created.
    - `POST` with an `agentId` failing the slug regex: 400, no job created, no filesystem access.
    - `POST` with a zero-byte, empty-named `File` (the browser's empty-file-input behavior, RESEARCH.md Pitfall 3): treated as NO file — `fileName` is `null`, nothing is written to `input/`, and the run uses the task text.
    - `POST` with a 201 KB file: 413, no job created, nothing written.
    - `POST` with a file named `../../escape.txt`: bytes land at `input/input.txt`; nothing exists outside `workDir(jobId)`.
    - `GET /jobs/:id` for a live job: body is exactly `{ status, events, error }` with no extra keys (no `agentId`, no `task`, no `fileName` leakage).
    - `GET /jobs/:id` for an unknown UUID: 404. For a malformed id (`'../etc'`, `'abc'`): 404, and no path helper is invoked.
    - `GET /jobs/:id/artifact` for a succeeded job: 200 with the exact file bytes, `Content-Type: text/plain; charset=utf-8`, and `Content-Disposition: attachment; filename="aic-job-<jobId>.txt"`.
    - `GET /jobs/:id/artifact` for a running/queued/failed job: 409. For an unknown id: 404. For a succeeded job whose output file was deleted: 404, not an unhandled ENOENT.
    - Two sequential POSTs: two distinct jobIds; polling each returns only its own events.
  </behavior>
  <action>
    Create the three route files at exactly these paths (this establishes the repo's first `src/routes/api/*` directory — there is no local precedent, so follow RESEARCH.md Pattern 1):
    - `src/routes/api/tryitout/jobs/+server.ts` exporting `POST`
    - `src/routes/api/tryitout/jobs/[id]/+server.ts` exporting `GET`
    - `src/routes/api/tryitout/jobs/[id]/artifact/+server.ts` exporting `GET`

    **POST handler**, in this order:
    1. `const form = await request.formData()`; read `agentId`, `task`, `file`.
    2. Validate `agentId` and `task` are non-empty strings and `agentId` passes `AgentIdSchema` (imported from `$lib/server/tryItOutPrompts`); `error(400, ...)` otherwise.
    3. Normalize the file: treat `form.get('file')` as a real upload only when it `instanceof File` AND `file.size > 0` AND `file.name !== ''` — otherwise coerce to `null` (RESEARCH.md Pitfall 3).
    4. Enforce `MAX_UPLOAD_BYTES = 200 * 1024` against `file.size`, and the same cap against `task.length`; `error(413, ...)` on breach. This must happen BEFORE `createJob`, before `mkdir`, and before `runJob` (threat T4-3).
    5. `const job = createJob(agentId, task, file)`.
    6. If a real file: `await mkdir(inputDir(job.jobId), { recursive: true })` then `await writeFile(inputFilePath(job.jobId), Buffer.from(await file.arrayBuffer()))`. Write to `inputFilePath` ONLY — never build a path from `file.name` (threat T4-2).
    7. Fire and forget: `runJob(job.jobId).catch(() => {})` with a comment stating `runJob` records its own `failed` status and that awaiting here would collapse the `queued`/`running` staging SC-04 requires.
    8. `return json({ jobId: job.jobId, status: job.status })`.

    **GET status handler:** guard with `isValidJobId(params.id)` -> `error(404, ...)`; `getJob(params.id)` -> `error(404, ...)` if absent; return `json({ status: job.status, events: job.events, error: job.error })`. Construct that object literally — do not spread the record, which would leak `task`/`fileName`/`agentId`.

    **GET artifact handler:** guard with `isValidJobId` -> 404; `getJob` -> 404 if absent; `error(409, ...)` unless `status === 'succeeded'`; `readFile(outputFilePath(params.id))` inside try/catch converting ENOENT to `error(404, ...)`; return `new Response(buf, { headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Content-Disposition': \`attachment; filename="aic-job-${params.id}.txt"\` } })`.

    Write `src/routes/api/tryitout/jobs/jobs.test.ts` importing the three handler functions directly and invoking them with plain objects shaped like `RequestEvent` (`{ request }` for POST, `{ params }` for GET) — SvelteKit route modules export ordinary functions, so no HTTP server is needed. Build multipart bodies with the standard `FormData`/`File` globals. Mock `openai` via `vi.mock('openai')`, and set `TRYITOUT_WORK_DIR` to an `os.tmpdir()` path created/removed per test. Assert thrown SvelteKit `error()` objects by their `status` property.
  </action>
  <verify>
    <automated>npx vitest run src/routes/api/tryitout/jobs/jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/routes/api/tryitout/jobs/jobs.test.ts` passes with at least one test per bullet in `<behavior>`.
    - The oversized-upload test asserts THREE things: response status 413, `getJob` returns undefined for any new id, and the work directory contains no new entries (proving the cap runs before any write — threat T4-3).
    - The traversal-filename test asserts the bytes are at `input/input.txt` and that `workDir(jobId)`'s recursive listing contains no `escape.txt` (threat T4-2).
    - `grep -n "await runJob" src/routes/api/tryitout/jobs/+server.ts` returns no match (fire-and-forget — RESEARCH.md Anti-Patterns).
    - `grep -n "\.\.\.job" src/routes/api/tryitout/jobs/\[id\]/+server.ts` returns no match (explicit `{ status, events, error }` only).
    - `grep -rn "file.name" src/routes/api/tryitout/` shows `file.name` used only in the empty-file check, never inside a `join(` or a path template.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>All three endpoints behave per `docs/job-api-contract.md`, with every id and filename that touches the filesystem validated first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire src/lib/tryItOut.ts to real fetch, with a shared fake-API test stub</name>
  <files>src/lib/tryItOut.ts, src/lib/tryItOut.test.ts, src/lib/testing/fakeJobApi.ts, src/lib/components/TryItOutPanel.test.ts</files>
  <read_first>
    - `src/lib/tryItOut.ts` (full current file — lines 1-25 frozen verbatim, lines 27-49 keep signatures, lines 51+ deleted; note `triggerTextDownload` at 147-156 and `safeFileToken` at 158-160 must be re-created)
    - `docs/job-api-contract.md` (the frozen signatures and the "Provisional: poll" transport decision)
    - `src/lib/tryItOut.test.ts` (the existing structure to replace: `// @vitest-environment jsdom` banner, `describe` per exported function, a dedicated `describe('unknown job handling')` block)
    - `.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-RESEARCH.md` Architecture Patterns -> Pattern 4 (the fetch-backed bodies)
    - `src/routes/api/tryitout/jobs/+server.ts` and siblings (Task 1 — the exact response shapes and status codes being consumed)
  </read_first>
  <behavior>
    With `globalThis.fetch` stubbed by `installFakeJobApi()`:
    - `submitJob('demo-rfi-triage', 'triage this', file)` POSTs `multipart/form-data` to `/api/tryitout/jobs` with fields `agentId`, `task`, `file`, and resolves to the parsed `{ jobId, status }`.
    - `submitJob` with `file` omitted or `null` sends no `file` field at all.
    - `submitJob` rejects with an Error mentioning the status code when the response is not ok.
    - `subscribeProgress` invokes `onUpdate` with each polled `JobUpdate`, keeps polling while status is `queued`/`running`, and stops polling once status is `succeeded` or `failed`.
    - The returned unsubscribe function stops all further polling and further `onUpdate` calls, and leaves no pending timer (`vi.getTimerCount() === 0` under fake timers — the Phase 6 cleanup assertion style).
    - Calling unsubscribe while a poll request is in flight causes no `onUpdate` call when that request resolves.
    - A 404 from the status route yields exactly one `onUpdate({ status: 'failed', events: [], error: 'unknown job: <id>' })` and stops polling — matching the Phase 6 mock's unknown-job string so the panel's behavior is unchanged.
    - `downloadArtifact(jobId)` GETs `/api/tryitout/jobs/<id>/artifact`, converts the response to a Blob, triggers an anchor download named `aic-job-<sanitized id>.txt`, removes the anchor from the DOM, and calls `URL.revokeObjectURL` exactly once.
    - `downloadArtifact` rejects with an Error mentioning the status code on a non-ok response.
  </behavior>
  <action>
    Rewrite `src/lib/tryItOut.ts` in place:
    - Preserve lines 1-25 verbatim except for updating the header comment, which currently says "Currently mock-backed" — change that sentence to state the client now calls the real `/api/tryitout/jobs*` routes, and keep the sentence declaring the signatures frozen by `docs/job-api-contract.md`.
    - Keep the three exported function signatures byte-identical.
    - Delete everything from the `MOCK IMPLEMENTATION` banner to end of file.
    - Implement the bodies per RESEARCH.md Pattern 4, with these specifics: `submitJob` builds a `FormData` and POSTs to `/api/tryitout/jobs`, appending `file` only when it is truthy. `subscribeProgress` uses a `POLL_MS = 1000` `setTimeout` loop with a `cancelled` flag checked both before each request and after each response; on a 404 it emits the `unknown job: ${jobId}` shape above and stops; on any other non-ok response it emits `{ status: 'failed', events: [], error: \`poll failed: ${res.status}\` }` and stops. It must also clear any pending timer on unsubscribe so `vi.getTimerCount()` reaches 0.
    - Re-create `triggerTextDownload`-equivalent logic for a fetched Blob and re-create `safeFileToken` verbatim; keep the `URL.revokeObjectURL(url)` call and the `// threat T3 — no object-URL leak` comment.
    - This file must import nothing from `$lib/server/*` and reference no API key (threat T4-4).

    Create `src/lib/testing/fakeJobApi.ts` exporting `installFakeJobApi(options?)` — a `vi.stubGlobal('fetch', ...)`-based stub that emulates the three routes over an in-memory script: it serves a configurable sequence of `JobUpdate`s for the status route, a configurable artifact body, and configurable failure injection (404 / non-ok / oversized). Give it an `uninstall()` and expose the recorded requests so tests can assert on method, URL, and FormData fields. This file is test-support only: it lives under `src/lib/testing/`, is imported by test files only, and its name does not end in `.test.ts` so vitest will not collect it as a suite.

    Rewrite `src/lib/tryItOut.test.ts` against the new implementation, keeping the existing file's conventions: the `// @vitest-environment jsdom` banner, a `describe` block per exported function, and a dedicated `describe('unknown job handling')` block. Replace the fake-timer-driven mock assertions with `installFakeJobApi()`; keep `vi.useFakeTimers()` since `subscribeProgress` still uses `setTimeout` for polling. Preserve the two download assertions that already exist and still apply: the `URL.revokeObjectURL` single-call check and the `aic-job-<id>.txt` filename pattern check.

    Migrate `src/lib/components/TryItOutPanel.test.ts` to the same stub. Do NOT change `TryItOutPanel.svelte`. Only the test file changes: install `installFakeJobApi()` in `beforeEach`, and update the assertions that hardcode Phase 6 mock strings (`'read data/sample.csv'`, `'agent settled (clean exit)'`, `'pi exited non-zero: required input file missing'`) to the stage lines the fake API now serves. Every other assertion — idle render, Run enable/disable, the red failed block, download button visibility, the XSS-shape check, the file input, the bounded feed, auto-scroll, re-run reset, and all four subscription-cleanup tests — must keep testing the same behavior. The transport-discipline test at lines 106-116 must pass UNCHANGED; do not weaken it.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts` passes.
    - `git diff --exit-code src/lib/components/TryItOutPanel.svelte src/routes/agents/\[slug\]/+page.svelte` reports NO changes (SC-07: UI components unchanged).
    - The transport-discipline test block in `TryItOutPanel.test.ts` is unmodified: `git diff src/lib/components/TryItOutPanel.test.ts` shows no change within the `describe('TryItOutPanel transport discipline')` block.
    - `sed -n '1,25p' src/lib/tryItOut.ts` still exports `JobStatus`, `JobEvent`, `JobUpdate`, and `SubmitResult` with the same field names and union members as `docs/job-api-contract.md`.
    - `grep -c "MOCK IMPLEMENTATION\|mockJobs\|runMockScript\|SUCCESS_SCRIPT\|pi exited non-zero" src/lib/tryItOut.ts` returns 0 (mock deleted wholesale).
    - `grep -cE "\\\$lib/server|OPENAI_API_KEY" src/lib/tryItOut.ts` returns 0 (threat T4-4).
    - `grep -c "revokeObjectURL" src/lib/tryItOut.ts` returns at least 1 (threat T4-8).
    - The unsubscribe test asserts `vi.getTimerCount() === 0` after unsubscribing mid-poll.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The three client functions reach the real backend over `fetch`, the mock is gone, and every Phase 6 UI test still passes against unchanged components.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression and build durability check</name>
  <files>(no new files — runs npm test, tsc, npm run build; fixes land in files listed above)</files>
  <read_first>
    - `package.json` (scripts: `test` = `vitest run`, `build` = `npm run db:push && npm run ingest && vite build`, `preview` = `node build`)
    - `vitest.config.ts` (include globs `src/**/*.test.ts` and `scripts/**/*.test.ts`; `environmentMatchGlobs` puts `src/lib/components/**` in jsdom, everything else node)
    - `src/routes/agents/try-it-out.test.ts` (renders the detail page — confirm it still passes with the rewired client)
    - `svelte.config.js` (adapter-node; the build output that must not embed server-only modules)
  </read_first>
  <action>
    Run `npm test` and resolve every failure. Expect three categories and fix them at the right layer:
    (a) tests asserting Phase 6 mock strings — update the test, not the production code;
    (b) tests that now need the fetch stub — install `installFakeJobApi()`;
    (c) genuine regressions in the new code — fix the source.
    Do not delete a failing test to make the suite green; if a Phase 6 assertion no longer has meaning under the real backend, replace it with the equivalent assertion against the new behavior and note the substitution in the summary.

    Run `npx tsc --noEmit` and resolve all new type errors.

    Run `npm run build` to confirm the SvelteKit production build succeeds with the new `src/routes/api/**` routes and the `openai` dependency. Then verify the SC-09 bundling guarantee against the real build output: search `build/client/` for `OPENAI_API_KEY` and for a distinctive string from `src/lib/server/tryItOutRunner.ts` (for example the `calling model…` stage line) and confirm neither appears. This is the strongest available proof that SvelteKit kept the server-only modules out of the client bundle.

    Confirm `.tryitout-work/` produced during test runs is git-ignored and that `git status --porcelain` shows only intended files.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; ! grep -rq "OPENAI_API_KEY" build/client/ &amp;&amp; echo "SC-09 client-bundle check passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with zero failing and zero skipped tests.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - `grep -rc "OPENAI_API_KEY" build/client/` finds no matches (SC-09 — proven against real build output, not just source).
    - A distinctive runner-only string (e.g. the `calling model…` stage line) is absent from `build/client/` (proves `src/lib/server/**` was not bundled client-side).
    - `git status --porcelain` lists no `.tryitout-work` entry.
    - The total test count is greater than or equal to the pre-plan count — no test was deleted to achieve green.
  </acceptance_criteria>
  <done>The whole suite is green against the real backend, the production build succeeds, and the server-only boundary is proven against actual build output.</done>
</task>

</tasks>

<verification>
- `npm test` fully green; `npx tsc --noEmit` clean; `npm run build` succeeds.
- `git diff --exit-code src/lib/components/ src/routes/agents/[slug]/+page.svelte` reports no source changes to UI components (SC-07).
- The three endpoints behave per `docs/job-api-contract.md`, including the 409-before-succeeded and 404-on-unknown branches (SC-06).
- No `OPENAI_API_KEY` and no server-module string appears in `build/client/` (SC-09).
</verification>

<success_criteria>
- SC-01/SC-02: POST mints a UUID jobId, writes the upload under `.tryitout-work/<jobId>/input/`, starts no process, and returns `queued` immediately.
- SC-04/SC-05: driving the real handlers end to end with a mocked model reaches `succeeded` with only `info`-typed stage events.
- SC-06: status and artifact routes work, and the artifact download returns the real generated content.
- SC-07: `src/lib/tryItOut.ts` is the only file that changed to swap mock -> real; the UI is byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/07-fake-demo-backend-no-container-pi-implementing-the-try-it-ou/07-04-SUMMARY.md` recording the final endpoint contract (paths, status codes, headers), the `installFakeJobApi` API, and any Phase 6 test assertion that was substituted rather than preserved.
</output>
