---
phase: 06-runnable-try-it-out-flow-mock-backed
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - .planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md
autonomous: false
requirements: []            # none assigned to Phase 6 — see success_criteria_covered
success_criteria_covered: [SC-2, SC-3, SC-4, SC-5]   # the visually/behaviorally-verifiable half of ROADMAP.md Phase 6
context_decisions: [D-05, D-10, D-13, D-14, D-15]

must_haves:
  truths:
    - statement: "A human sees the Try It Out panel on a runnable agent's detail page in a real browser, runs a task, and watches timestamped pi-style lines stream into an auto-scrolling log box"
      verification: backstop
    - statement: "A human runs a task containing the word 'fail' and sees the red 'Job failed' block with the error string"
      verification: backstop
    - statement: "A human runs a normal task, clicks 'Download results', and receives a text file naming their task"
      verification: backstop
    - statement: "A human confirms the progress feed stays height-capped and scrolls internally instead of growing the page (UI-SPEC overflow backstop), and that a long task/summary line wraps instead of scrolling horizontally (UI-SPEC long-text backstop)"
      verification: backstop
    - statement: "A human confirms external mode still renders a working link and none mode still renders nothing — Phase 5 behavior is intact"
      verification: backstop
    - "COVERAGE.md exists in the phase directory and declares, with reasoning and verified greps, that this phase integrates no external API"
  artifacts:
    - path: ".planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md"
      provides: "Reasoned no-external-API declaration for the api-coverage detector false positive"
      contains: "No external API integration"
  key_links:
    - from: "dev DB agents row (one agent set to try_it_out_mode='runnable')"
      to: "src/routes/agents/[slug]/+page.svelte runnable branch"
      via: "+page.server.ts db.select() -> agent.tryItOutMode -> <TryItOutPanel agentId={agent.slug} />"
      pattern: "runnable"
---

<objective>
Close out Phase 6: record the reasoned no-external-API coverage declaration, stage a `runnable` agent in the local dev DB so the flow is actually demoable, and get human eyes on both outcomes in a real browser.

Purpose: four of the six ROADMAP Phase 6 success criteria are visual/behavioral (feed authenticity, red failed block, download, panel-on-detail-page). Automated tests assert structure, strings and classes; only a human can confirm it reads as an authentic live tool-call log and that the log box is genuinely capped and wrapping (the two UI-SPEC backstop items).

Output: `COVERAGE.md`, a staged runnable demo row, and human sign-off.
</objective>

<execution_context>
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/workflows/execute-plan.md
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-UI-SPEC.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-01-SUMMARY.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-02-SUMMARY.md
@.planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-01-SUMMARY.md
@src/lib/tryItOut.ts
@src/lib/components/TryItOutPanel.svelte
@src/routes/agents/[slug]/+page.svelte
</context>

<assumption_delta_decision>
Carried from 06-01/06-02, unchanged: **no-change**. Wired-vs-standalone is one component in two contexts (single defaulted `agentId` prop, no fork per D-13); the optional file is a nullable single value. No new route was created for standalone use — it is verified by the no-props render test in 06-01. If the human checkpoint below asks for a dedicated demo route or a "remove selected file" affordance, capture it as a follow-up item, not as in-phase scope.
</assumption_delta_decision>

<reversibility>
| Decision | Reversibility | Gate needed? |
|----------|---------------|--------------|
| `COVERAGE.md` (new planning doc) | Fully reversible — delete the file | No |
| One-off `UPDATE agents SET try_it_out_mode='runnable'` on the local, gitignored dev DB | Fully reversible — set it back to `'none'`; `npm run dev` regenerates the DB anyway | No |

**Confirmed: no one-way doors.** No schema file, ingest script, spec file, YAML source, dependency or external service is touched (D-15). The only mutation outside `.planning/` is one column value on one row of a gitignored local SQLite file.
</reversibility>

<threat_model>
ASVS Level 1, block-on high. This plan writes one markdown file, mutates one local dev-DB row, and asks a human to click around a `localhost` dev server.

| # | Threat | Severity | Control |
|---|--------|----------|---------|
| T1 | The human verifier attaches a real, sensitive file during the checkpoint | low | The checkpoint script instructs using a throwaway file (any scratch `.csv`, or `data/agents/rfi-triage-assistant.yaml`). The mock never reads file bytes — only `file.name` — and nothing is transmitted anywhere. Stated in the checkpoint copy. |
| T2 | XSS in the failed block reaching a human-visible page | **high** | Already controlled and asserted in 06-01/06-02 (escaped `{error}` interpolation, `{@html}` prohibited by source assertion). The checkpoint re-verifies visually that the error renders as plain text. |
| T3 | A leaked mock timer keeps firing after navigating away from the detail page | **high** | Already controlled and asserted in 06-02 (T2.17-T2.20). The checkpoint adds a human-observable check: start a job, navigate back to the catalog mid-run, confirm the browser console stays clean. |
| T4 | Dev-DB mutation accidentally committed | low | `db/` is gitignored (verified: `db/catalog.db` is untracked). The `UPDATE` is a one-off command recorded in the SUMMARY for reproducibility, never a file edit. |

Not applicable, deliberately not fabricated: authn/authz, CSRF, SQLi (a parameterless one-off UPDATE the developer runs on their own local file), secrets management, rate limiting — none of these surfaces exists in this phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write COVERAGE.md and stage a runnable agent in the dev DB</name>

  <files>.planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md</files>

  <read_first>
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md (D-02, D-08 — the reasoning behind the no-external-API declaration)
    - docs/job-api-contract.md (its "Real backend endpoints (target the mock emulates)" section — a FUTURE target, not an integration)
    - src/lib/tryItOut.ts (confirm for yourself there is no `fetch`/`EventSource`/URL literal before declaring it)
    - .planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-01-SUMMARY.md (the documented one-off `better-sqlite3` UPDATE procedure and its prerequisites — reuse verbatim)
    - drizzle/schema.ts lines 21-26 (READ ONLY — confirm `try_it_out_mode` is `notNull().default('none')`; do not edit)
  </read_first>

  <action>
**Part A — write `COVERAGE.md`.**

Create `.planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md`. The api-coverage detector flagged the keyword "agent" (from `agentId` / "agent detail page") — a false positive. Do NOT fabricate a capability matrix. Required content:

```markdown
# Phase 06 — API Coverage Declaration

**Verdict: No external API integration.**

This phase integrates no external API, SDK, or service. It mocks a *future* backend
entirely client-side with in-memory `setTimeout` timers.

## Why the detector fired
The keyword "agent" appears throughout this phase (`agentId`, "agent detail page")
because the product domain is a catalog of AI agents. No agent runtime, LLM API, or
HTTP service is called.

## Evidence
- `src/lib/tryItOut.ts` contains no `fetch`, `EventSource`, `XMLHttpRequest`, or URL
  literal — verified: `grep -nE "fetch\(|EventSource|XMLHttpRequest|https?://" src/lib/tryItOut.ts` (no matches).
- `src/lib/components/**` and `src/routes/**` contain none either (06-CONTEXT.md D-02,
  ROADMAP Phase 6 Success Criterion 6) — verified by the same grep and by a source
  assertion test in `src/lib/components/TryItOutPanel.test.ts`.
- All job state lives in a module-level `Map` inside `tryItOut.ts`, below the
  "MOCK IMPLEMENTATION — DELETE WHOLESALE" banner (D-08). It does not survive a reload.
- No dependency was added to `package.json` in this phase.

## The endpoints named in docs/job-api-contract.md
`POST /jobs`, `GET /jobs/:id`, `GET /jobs/:id/events`, `GET /jobs/:id/artifact` are the
*future* target the mock is shaped against. They are documented so the later runtime
phase can swap implementations behind the three frozen client functions. None of them
is called, referenced, or configured by any code in this phase.

## When this changes
The future runtime phase (in-process `pi --mode rpc` module — see the
`try-it-out-runtime-architecture` project memory) is when a real integration lands.
That phase — not this one — owns the API coverage matrix, auth, error taxonomy,
timeout/retry policy, and rate-limit handling.
```

Run every grep quoted above and confirm it really returns nothing BEFORE writing the file. If any grep matches, stop and report — the declaration must be true.

**Part B — stage a `runnable` agent so the checkpoint is demoable.**

The local dev DB (`db/catalog.db`, gitignored) currently has NO `try_it_out_*` columns — it was reset since Phase 5. Reproduce the documented Phase 5 procedure, then set ONE agent to `runnable`, keeping `rfi-triage-assistant` as the `external` example so all three modes are visible:

1. `npm run db:push` — applies the `try_it_out_*` columns already defined in `drizzle/schema.ts`. This is a DB-state operation only; `drizzle/schema.ts` is NOT edited (D-15).
2. `npm run ingest` — ensures every agent row exists.
3. Stage the modes:

```bash
node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db');d.prepare(\"UPDATE agents SET try_it_out_mode='runnable' WHERE slug='hvac-load-calculator'\").run();d.prepare(\"UPDATE agents SET try_it_out_mode='external', try_it_out_url='https://example.com/try/rfi-triage-assistant' WHERE slug='rfi-triage-assistant'\").run();d.close()"
```

4. Confirm:

```bash
node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db',{readonly:true});console.log(d.prepare('select slug,try_it_out_mode,try_it_out_url from agents order by slug').all())"
```

Expected: `hvac-load-calculator` -> `runnable`, `rfi-triage-assistant` -> `external` with the placeholder URL, every other agent -> `none`.

If `npm run ingest` fails for a reason unrelated to Phase 6, record it and stage the rows anyway — the panel only needs the row's `try_it_out_mode` value. (Note: `scripts/ingest.test.ts` failing under `npx vitest run` is a known Phase 5 regression owned by the unexecuted plan 05-02; the ingest *script* itself works against the real DB once `db:push` has run.)

**PROHIBITIONS:**
- Do NOT edit `drizzle/schema.ts`, `scripts/ingest.ts`, anything under `src/lib/spec/`, or any YAML under `data/agents/` (D-15 — the mode is set by a one-off DB UPDATE, exactly as Phase 5 did).
- Do NOT commit anything under `db/` (gitignored).
- Do NOT fabricate an API capability matrix, auth section, or rate-limit table in COVERAGE.md.
  </action>

  <verify>
    <automated>test -f .planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md && grep -q "No external API integration" .planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md && node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db',{readonly:true});const r=d.prepare('select slug,try_it_out_mode from agents').all();const has=(m)=>r.some(x=>x.try_it_out_mode===m);if(!has('runnable')||!has('external'))process.exit(1);console.log('staged ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `COVERAGE.md` exists in the phase directory, contains the literal string `No external API integration`, and contains no capability matrix / auth / rate-limit table.
    - `grep -rnE "fetch\(|EventSource|XMLHttpRequest" src/lib/tryItOut.ts src/lib/components src/routes` returns no matches (the declaration's central claim is literally true).
    - The dev DB query in step 4 shows at least one agent with `try_it_out_mode='runnable'` and `rfi-triage-assistant` with `try_it_out_mode='external'` plus a non-null `try_it_out_url`.
    - `git status --porcelain` shows no changes under `drizzle/`, `scripts/`, `src/lib/spec/`, `data/`, or `db/`.
    - The exact `UPDATE` commands used are copied into the SUMMARY for reproducibility after any future DB reset.
  </acceptance_criteria>

  <done>The no-external-API declaration is written and factually verified by grep, and the local dev DB has one `runnable` agent plus the Phase 5 `external` example staged so all three Try It Out modes can be demoed in one browser session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — both outcomes, in a real browser</name>

  <files>none — verification only; no file is modified unless a defect is found</files>

  <read_first>
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-UI-SPEC.md ("UI Considerations" table — the `overflow` and `long-text` rows are the two backstop items steps 6 and 7 exist to close)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-01-SUMMARY.md and 06-02-SUMMARY.md (what was actually built, and any deviation to mention to the human)
    - src/lib/components/TryItOutPanel.svelte (so you can answer questions about the rendered states without guessing)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md D-14 (what must NOT have changed: external / none behavior)
  </read_first>

  <action>
    Automation is already complete — do NOT ask the human to do anything Claude can do.

    Before pausing:
    1. Run the automated baseline in `<verify>` below and confirm it is green. If it is not, fix it first; do not hand a broken build to the human.
    2. Confirm the dev DB is staged (Task 1 Part B) — at least one `runnable` agent and the `external` example.
    3. Start the dev server yourself: `npm run dev` (background). Confirm it is listening (default `http://localhost:5173`) and report the actual port and the two URLs the human needs:
       - runnable: `http://localhost:5173/agents/hvac-load-calculator`
       - external: `http://localhost:5173/agents/rfi-triage-assistant`
       If `npm run dev` re-ran `db:push`/`ingest` and reset the staged modes, re-apply the two `UPDATE` commands from Task 1 Part B step 3 BEFORE pausing, and re-confirm with the read-only query.
    4. Present the numbered script in `<how-to-verify>` verbatim, with the real port substituted, then STOP and wait for the resume signal.

    After the human responds:
    - "approved" -> write the SUMMARY per `<output>` and close the phase.
    - defects -> fix only within this phase's file scope (`src/lib/tryItOut.ts`, `src/lib/components/TryItOutPanel.svelte`, `src/routes/agents/[slug]/+page.svelte` and their tests), add a regression test for each fix, re-run the baseline, and re-present the affected steps. Scope-expansion requests (standalone demo route, remove-file chip, cancel/retry, real execution) go to the SUMMARY as follow-ups — do not implement them here (D-15 and the deferred list in 06-CONTEXT.md).
  </action>

  <verify>
    <automated>npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts src/routes/agents/detail.test.ts && npx vite build</automated>
  </verify>

  <what-built>
    Automation is complete before this checkpoint. Already built and verified by tests:
    - `src/lib/tryItOut.ts` — the three frozen client functions (`submitJob`, `subscribeProgress`, `downloadArtifact`) + the four contract types, mock-backed, all mock internals below a delete-wholesale banner.
    - `src/lib/components/TryItOutPanel.svelte` — Task textarea, optional file input, Run button ("Running…" while in flight), bounded auto-scrolling monospace progress feed, "Download results" on success, red "Job failed" block on failure.
    - `src/routes/agents/[slug]/+page.svelte` — the runnable branch now renders the panel instead of Phase 5's disabled button.
    - Test suites: `src/lib/tryItOut.test.ts`, `src/lib/components/TryItOutPanel.test.ts`, `src/routes/agents/try-it-out.test.ts` (all green), plus `npx vite build` succeeding.
    - Dev DB staged: `hvac-load-calculator` = `runnable`, `rfi-triage-assistant` = `external`, everything else = `none`.

    What tests CANNOT confirm, and why you are being asked: whether the feed *reads* as an authentic live tool-call log (SC-2), whether the log box is genuinely height-capped and internally scrolling rather than growing the page (UI-SPEC `overflow` backstop), whether long lines wrap rather than overflow horizontally (UI-SPEC `long-text` backstop), and whether the download actually lands as a file in your browser.
  </what-built>

  <how-to-verify>
    Start the dev server: `npm run dev` (it re-runs `db:push` + `ingest`; if it reports that the staged modes were reset, re-run the two `UPDATE` commands from Task 1 Part B step 3).

    1. **Panel is wired in (SC-5).** Visit `http://localhost:5173/agents/hvac-load-calculator`. Expect: below the "View on GitHub" row, a white card with a "Try it out" heading, a "Task" textarea, an "Attach a file (optional)" input, and an indigo "Run" button. Expect NO greyed-out disabled "Try it out" button anywhere (Phase 5's placeholder is gone).
    2. **Success path (SC-2, SC-3).** Type `summarize the sample csv` and click Run. Expect: the button reads "Running…" and is unclickable; a "PROGRESS" label appears; monospace lines stream in over ~4 seconds with `HH:MM:SS` timestamps — `read data/sample.csv`, then `read data/sample.csv (12 lines)`, then `count rows in data/sample.csv`, then `write output/result.txt`, then `agent settled (clean exit)`. Judge: does this read like a real tool-call log? Then an indigo "Download results" button appears and the Run button returns to "Run".
    3. **Download (SC-3).** Click "Download results". Expect a `.txt` file download whose contents include your task text (`summarize the sample csv`) and the agent slug.
    4. **File attachment.** Attach any throwaway file (e.g. a small scratch `.csv` — do NOT use anything sensitive; nothing is uploaded anywhere, but the filename appears in the artifact). Run again, download, and confirm the artifact's `attached file:` line names your file.
    5. **Failure path (SC-4).** Type `make this fail` and click Run. Expect: after ~4 seconds a red block appears with "Job failed", the error `pi exited non-zero: required input file missing`, and "Adjust your task and try again." Expect NO "Download results" button. Confirm the error text renders as plain text (no raw HTML, no broken markup).
    6. **Overflow backstop.** Run several jobs back to back without reloading (or run one and resize the window small). Expect: the log box keeps a fixed maximum height and scrolls inside itself; the page does NOT grow indefinitely; the newest line stays visible without you scrolling.
    7. **Long-text backstop.** Paste a very long single-line task (300+ characters with no spaces, then one with spaces) and Run. Expect: the textarea and the feed lines wrap; no horizontal scrollbar appears on the panel or the page.
    8. **Cleanup (threat T3).** Open the browser devtools console. Start a job and, while it is still running, click "← Back to catalog". Expect: the console stays clean — no errors, no Svelte warnings about updating a destroyed component.
    9. **Phase 5 regression check (D-14).** Visit `http://localhost:5173/agents/rfi-triage-assistant` — expect the inline "Try it out →" link (external mode) pointing at `https://example.com/try/rfi-triage-assistant` and NO panel. Then visit any other agent (e.g. `/agents/mep-scope-planner`) — expect no Try It Out affordance at all.
    10. **Tablet viewport (UI-01).** Narrow the window to ~820px wide. Expect the panel and its controls to remain usable with no horizontal page scrolling.

    Report anything that looks wrong, especially: feed lines that do not read as authentic, a log box that grows the page, horizontal scrollbars, console errors after navigating away, or a download that does not contain your task.
  </how-to-verify>

  <resume-signal>Type "approved" to close Phase 6, or describe what looked wrong (which step number, what you saw vs. expected).</resume-signal>

  <acceptance_criteria>
    - A human has executed all 10 steps against a running dev server and explicitly approved, OR reported specific defects.
    - Any reported defect is either fixed within this phase's file scope (`src/lib/tryItOut.ts`, `src/lib/components/TryItOutPanel.svelte`, `src/routes/agents/[slug]/+page.svelte` and their tests) with a regression test added, or recorded as a follow-up item in the SUMMARY with a reason for deferral.
    - Steps 6 and 7 (the two UI-SPEC backstop items) are explicitly confirmed or defected — they must not be silently skipped.
    - Requests that expand scope (a dedicated standalone demo route, a "remove selected file" chip, a cancel/retry button, real execution) are recorded as follow-ups, not implemented here.
  </acceptance_criteria>

  <done>A human has run both the success and failure paths in a real browser, downloaded a real artifact, confirmed the feed reads as an authentic live tool-call log, confirmed the two UI-SPEC backstop behaviors, confirmed a clean console after navigating away mid-job, and confirmed Phase 5's external/none behavior is intact.</done>
</task>

</tasks>

<verification>
Before presenting the checkpoint, confirm the automated baseline is green:

1. `npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts src/routes/agents/detail.test.ts` -> 0 failures.
2. `npx vite build` -> succeeds.
3. `grep -rnE "fetch\(|EventSource|XMLHttpRequest|\{@html" src/lib/components src/routes` -> no output (SC-6, D-02).
4. `grep -n "^export" src/lib/tryItOut.ts` -> exactly 3 functions + 4 types (SC-1, D-01).
5. `git status --porcelain | grep -E "^.. (drizzle/|scripts/|src/lib/spec/|data/)"` -> no output (D-15).
6. Dev DB staged: at least one `runnable` agent and one `external` agent.

Known, expected, out-of-scope failure: the whole-suite `npx vitest run` still fails in `scripts/ingest.test.ts` (Phase 5 regression owned by the unexecuted plan 05-02, documented in `.planning/phases/05-*/deferred-items.md`). Do not fix it here.
</verification>

<success_criteria>
All six ROADMAP Phase 6 success criteria are now demonstrably true:
1. `src/lib/tryItOut.ts` exposes exactly the three mock-backed, contract-shaped functions; a real-backend swap touches only this file (grep-verified export surface + the delete-wholesale banner).
2. Mock progress events are the contract's verbatim pi `tool_execution` lines with `HH:MM:SS` timestamps — human-confirmed as reading like a real log.
3. The panel renders the task textarea, optional file input, Run button, live auto-scrolling progress feed, "Download results" on success, and a red failed block with the error — human-confirmed in a browser.
4. A task containing "fail" reaches the failed path; both outcomes are demoable with no code changes — human-confirmed.
5. The panel is wired into the runnable-mode detail page (human-confirmed) and works standalone with zero props (test-confirmed in 06-01).
6. No real backend exists; no `fetch`/`EventSource`/endpoint reference appears anywhere in the UI — grep-verified and declared in `COVERAGE.md`.
</success_criteria>

<artifacts_produced>
**This plan produces:**
- `.planning/phases/06-runnable-try-it-out-flow-mock-backed/COVERAGE.md` — reasoned no-external-API declaration (no capability matrix; explicitly assigns API coverage ownership to the future runtime phase).
- A staged local dev-DB state: one `runnable` agent (`hvac-load-calculator`), the Phase 5 `external` example (`rfi-triage-assistant`), all others `none`. Not a code artifact — the exact `UPDATE` commands are recorded in the SUMMARY for reproducibility.
- Human sign-off on the four visually/behaviorally-verifiable success criteria and the two UI-SPEC backstop items.

**No source file is created or modified by this plan** unless the checkpoint surfaces a defect, in which case fixes are confined to `src/lib/tryItOut.ts`, `src/lib/components/TryItOutPanel.svelte`, `src/routes/agents/[slug]/+page.svelte` and their test files.

**Full phase symbol inventory (produced across 06-01 and 06-02):** `JobStatus`, `JobEvent`, `JobUpdate`, `SubmitResult`, `submitJob`, `subscribeProgress`, `downloadArtifact` (all from `src/lib/tryItOut.ts`); `TryItOutPanel` with props `{ agentId?: string }` (default `'demo-agent'`) from `src/lib/components/TryItOutPanel.svelte`; test files `src/lib/tryItOut.test.ts` and `src/lib/components/TryItOutPanel.test.ts`; modified `src/routes/agents/[slug]/+page.svelte` and `src/routes/agents/try-it-out.test.ts`.
</artifacts_produced>

<output>
After completion, create `.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-03-SUMMARY.md`.

Record explicitly:
- The verbatim `UPDATE` commands used to stage the runnable/external demo rows (needed again after any DB reset).
- The human's verdict per checkpoint step, especially steps 6 and 7 (the UI-SPEC overflow and long-text backstops).
- Any defect found and how it was fixed, or why it was deferred.
- Any scope-expansion request captured as a follow-up (standalone demo route, remove-file chip, cancel/retry).
- A closing note that the future runtime phase replaces only the bodies of the three functions in `src/lib/tryItOut.ts` plus everything below its MOCK IMPLEMENTATION banner — no UI change required.
</output>
</content>
