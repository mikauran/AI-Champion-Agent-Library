---
phase: 06-runnable-try-it-out-flow-mock-backed
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/components/TryItOutPanel.svelte
  - src/lib/components/TryItOutPanel.test.ts
autonomous: true
requirements: []            # none assigned to Phase 6 — see success_criteria_covered
success_criteria_covered: [SC-3, SC-4, SC-6]   # ROADMAP.md Phase 6 Success Criteria numbering
context_decisions: [D-02, D-06, D-07, D-10, D-11, D-12, D-13, D-15]

must_haves:
  truths:
    - "A task containing the word 'fail' drives the panel into a red 'Job failed' block that shows the verbatim JobUpdate.error string"
    - "A successful job reveals a 'Download results' button that is absent in every other state"
    - "Clicking 'Download results' triggers a browser download of a text artifact containing the submitted task"
    - "An optional file can be attached and its name reaches the client — provable end-to-end because the downloaded artifact names the attached file"
    - "The progress feed is a bounded, monospace, auto-scrolling log box — long event lists scroll inside it instead of growing the page, and long summary lines wrap instead of overflowing horizontally"
    - "Unmounting the panel mid-job stops all further mock timers — no state update or timer callback fires after destroy"
    - "Re-running after a terminal state clears the previous events, error and download button before the new job starts"
    - "The panel still contains no fetch, EventSource, endpoint literal or {@html}"
  artifacts:
    - path: "src/lib/components/TryItOutPanel.svelte"
      provides: "Complete UI-SPEC surface: file input, in-flight state, bounded auto-scrolling feed, Download results button, red failed block"
      contains: "Download results"
      min_lines: 90
    - path: "src/lib/components/TryItOutPanel.test.ts"
      provides: "Tests for the fail path, download, file attachment, overflow/wrap classes, unmount cleanup and re-run reset"
      contains: "unmount"
  key_links:
    - from: "TryItOutPanel file input onchange"
      to: "submitJob(agentId, task, file)"
      via: "$state file variable passed as the third argument"
      pattern: "submitJob\\(agentId, task, file\\)"
    - from: "Download results button"
      to: "downloadArtifact(jobId)"
      via: "onclick with the jobId captured from SubmitResult"
      pattern: "downloadArtifact\\("
    - from: "JobUpdate.error"
      to: "red failed-state block"
      via: "escaped Svelte interpolation {error} inside bg-red-50/border-red-200/text-red-700"
      pattern: "bg-red-50"
---

<objective>
Expand the verified tracer panel into the full 06-UI-SPEC.md surface: optional file input, the red "Job failed" block, the "Download results" button, and the bounded auto-scrolling monospace log box — plus the tests that prove the failure path, the download, the file hand-off, and that unmounting mid-job leaks no timers.

Purpose: 06-01 proved the seam works for the happy path. This plan makes both demo outcomes reachable without code changes (SC-4) and closes the two UI-SPEC backstop items (overflow, long-text). `src/lib/tryItOut.ts` is already complete and frozen — this plan does not touch it.

Output: the finished `TryItOutPanel.svelte` and its complete test suite.
</objective>

<execution_context>
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/workflows/execute-plan.md
@/home/ville/dev/aic-tryitout/AI-Champion-Agent-Library/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-UI-SPEC.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-PATTERNS.md
@.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-01-SUMMARY.md
@docs/job-api-contract.md
@src/lib/tryItOut.ts
@src/lib/components/TryItOutPanel.svelte
@src/lib/components/TryItOutPanel.test.ts
@src/lib/components/FilterBar.svelte
</context>

<interfaces>
<!-- Already built by 06-01. Do not re-derive, do not modify. -->

```ts
// src/lib/tryItOut.ts — FROZEN (D-01). Exactly these exports exist.
export type JobStatus = 'queued' | 'running' | 'succeeded' | 'failed'
export interface JobEvent { ts: string; type: 'tool_start' | 'tool_end' | 'info'; tool?: string; summary: string }
export interface JobUpdate { status: JobStatus; events: JobEvent[]; error: string | null }
export interface SubmitResult { jobId: string; status: JobStatus }
export function submitJob(agentId: string, task: string, file?: File | null): Promise<SubmitResult>
export function subscribeProgress(jobId: string, onUpdate: (u: JobUpdate) => void): () => void
export function downloadArtifact(jobId: string): Promise<void>
```

Mock behavior facts you must test against (fixed by 06-01):
- `submitJob` resolves at 400ms; terminal update lands at 4000ms after `subscribeProgress`.
- Fail trigger regex: `/(^|\W)fail(\W|$)/i` on the task. `'please fail now'` fails; `'failure'` does NOT.
- Fail error string, verbatim: `pi exited non-zero: required input file missing`
- Success feed lines, in order: `read data/sample.csv`, `read data/sample.csv (12 lines)`, `count rows in data/sample.csv`, `write output/result.txt`, `agent settled (clean exit)`
- Fail feed lines, in order: `read data/sample.csv`, `read data/sample.csv (12 lines)`, `agent failed (exit 1)`
- Artifact text includes the jobId, agentId, the task, and `attached file: <name>` (or `(none)`).
- jsdom lacks `URL.createObjectURL`/`revokeObjectURL` — stub them in any download test.

06-UI-SPEC.md copy this plan must use verbatim: `Attach a file (optional)`, `Download results`, `Job failed`, `Adjust your task and try again.`
06-UI-SPEC.md colors for the failed block: `bg-red-50` / `border-red-200` / `text-red-700`. Accent `bg-indigo-600` for Download results. NO green success badge — the Download button's appearance IS the success signal.
</interfaces>

<assumption_delta_decision>
Carried from 06-01, unchanged: **no-change**. The `agentId` prop with a default is one identity in two contexts (wired / standalone), not a singular→plural shift; the optional file is a nullable single value (`file?: File | null`, frozen in the contract), not a collection. Per 06-UI-SPEC.md's one unresolved item, the native `<input type="file">` single-file replace-on-reselect behavior is accepted as sufficient — **no custom "remove selected file" chip is built** in this plan. If the 06-03 checkpoint finds it insufficient, that becomes a follow-up, not a scope expansion here.
</assumption_delta_decision>

<reversibility>
| Decision | Reversibility | Gate needed? |
|----------|---------------|--------------|
| All edits are additive markup + tests inside two already-new files | Fully reversible — `git checkout` the two files | No |
| Native file input with no removal affordance | Reversible — adding a removal chip later is purely additive | No |
| `max-h-64` feed height cap | Reversible — a one-token class change | No |

**Confirmed: no one-way doors.** No schema, migration, dependency, data write or external service is involved. Nothing gated.
</reversibility>

<threat_model>
ASVS Level 1, block-on high. Same honest scope as 06-01 (client-side mock only), but this plan is where the two high-severity controls actually get exercised in the UI:

| # | Threat | Severity | Control (enforced by acceptance criteria) |
|---|--------|----------|-------------------------------------------|
| T1 | XSS via the error string — the failed block is the first place an untrusted-in-future string (real `pi` stderr / backend error) is rendered | **high** | Render with `{error}` only. `{@html}` is PROHIBITED; asserted by the source test from 06-01 which must keep passing. Test additionally asserts an HTML-looking error string would be shown as text (see T2.10). |
| T2 | Timer/memory leak after unmount — the user navigates away mid-job and the mock keeps firing `setTimeout`s into a destroyed component | **high** | `$effect` teardown calls unsubscribe (built in 06-01); this plan PROVES it with an unmount test asserting `vi.getTimerCount() === 0` and no post-unmount callbacks. |
| T3 | Object-URL leak on repeated downloads | medium | Already controlled inside `downloadArtifact` (06-01 revokes immediately). This plan asserts the button can be clicked twice without accumulating un-revoked URLs (`revokeObjectURL` call count === `createObjectURL` call count). |
| T4 | Stale download offered for a superseded job (clicking Download after starting a new run) | medium | `jobId` is reset to `null` and `status` to `'queued'` at the start of every run, so the Download button unmounts before the new job begins. Asserted by the re-run reset test. |
| T5 | File contents read/logged in the browser | low | The panel only passes the `File` object through to `submitJob`; no `FileReader`, no `console.log`. Asserted by source grep. |
| T6 | Transport detail leaking into the component | medium | `grep -nE "fetch\(|EventSource|XMLHttpRequest|https?://"` on the component must stay empty (SC-6, D-02). |

Not applicable, deliberately not fabricated: authn/authz, CSRF, SQLi, secrets, rate limiting — no server, no DB, no credentials exist in this phase.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the failed-state block and the Download results button</name>

  <files>src/lib/components/TryItOutPanel.svelte, src/lib/components/TryItOutPanel.test.ts</files>

  <read_first>
    - src/lib/components/TryItOutPanel.svelte (the 06-01 tracer version — you are extending it, not rewriting it)
    - src/lib/components/TryItOutPanel.test.ts (the 06-01 suite — reuse its fake-timer helper approach exactly)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-01-SUMMARY.md (states WHICH fake-timer approach worked — reuse it, do not re-litigate)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-UI-SPEC.md (Copywriting Contract rows "Secondary CTA", "Error state heading", "Error state body"; Color row "Destructive/semantic")
    - src/lib/tryItOut.ts (the `downloadArtifact` signature and the artifact text it produces)
  </read_first>

  <behavior>
    Tests to ADD to `src/lib/components/TryItOutPanel.test.ts`:

    - T2.6 Fail path: type `'please fail now'`, click Run, advance past 4000ms → `container.textContent` contains `Job failed`, contains `pi exited non-zero: required input file missing`, and contains `Adjust your task and try again.`; the failed container element (`.bg-red-50`) is present; NO button reading `Download results` exists; the feed shows `read data/sample.csv (12 lines)` and `agent failed (exit 1)`.
    - T2.7 Success reveals download: type `'summarize the csv'`, run to terminal → a `<button>` with trimmed text exactly `Download results` exists, and no `.bg-red-50` element exists.
    - T2.8 No download button in non-terminal states: assert absent at idle, and absent immediately after clicking Run (before 4000ms).
    - T2.9 Download click triggers a real artifact: with `URL.createObjectURL = vi.fn(() => 'blob:mock')`, `URL.revokeObjectURL = vi.fn()` and `vi.spyOn(HTMLAnchorElement.prototype, 'click').mockImplementation(() => {})`, run a job to `succeeded`, `await fireEvent.click(downloadButton)` → `createObjectURL` called once, `await blob.text()` contains `'summarize the csv'`, anchor `click` called once, and `revokeObjectURL` call count equals `createObjectURL` call count (threat T3). Click Download a second time and assert the counts still match.
    - T2.10 XSS-shape assertion (threat T1): the failed block renders text, never markup — after the fail flow, assert `container.querySelector('.bg-red-50')!.querySelector('script')` is `null` and that the error text is reachable via `textContent` (i.e. escaped interpolation is in use). Also re-assert `expect(src).not.toMatch(/\{@html/)` on the component source.
    - T2.11 Re-run reset (threat T4): run a failing job to terminal, then type `'summarize the csv'` and click Run again → immediately after the click the `.bg-red-50` block is gone and the feed no longer contains `agent failed (exit 1)`; after advancing to terminal, `Download results` is present.
  </behavior>

  <action>
Extend `src/lib/components/TryItOutPanel.svelte`. Add `downloadArtifact` to the existing import (it stays the same single import from `$lib/tryItOut`):

```svelte
import { submitJob, subscribeProgress, downloadArtifact } from '$lib/tryItOut'
```

Insert AFTER the `{#if status !== 'idle'} … {/if}` progress-feed block, inside the `<aside>`:

```svelte
{#if status === 'succeeded' && jobId}
  <button
    type="button"
    onclick={() => downloadArtifact(jobId!)}
    class="mt-4 inline-flex items-center gap-2 min-h-[44px] rounded bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-700 focus-visible:ring-2 focus-visible:ring-indigo-600"
  >
    Download results
  </button>
{/if}

{#if status === 'failed'}
  <div class="mt-4 rounded border border-red-200 bg-red-50 p-4">
    <p class="text-xs font-semibold text-red-700">Job failed</p>
    <p class="mt-1 text-sm text-red-700">{error}</p>
    <p class="mt-1 text-sm text-red-700">Adjust your task and try again.</p>
  </div>
{/if}
```

Exact copy comes from 06-UI-SPEC.md's Copywriting Contract (`Download results`, `Job failed`, `Adjust your task and try again.`) and its Color row (`border-red-200` / `bg-red-50` / `text-red-700` for destructive; `bg-indigo-600` accent for the secondary CTA). Heading weight `text-xs font-semibold` is the Label typography role; body `text-sm` is the Body role.

The re-run reset (`events = []`, `error = null`, `jobId = null`, `status = 'queued'`) already exists at the top of `run()` from 06-01 — verify it is there and do not duplicate it. It is what makes T2.11 and threat T4 pass.

**PROHIBITIONS:**
- Do NOT add a green success badge or any third semantic color — 06-UI-SPEC.md explicitly rejected it; the Download button's appearance IS the success signal.
- Do NOT use `{@html}` for `error` (threat T1).
- Do NOT modify `src/lib/tryItOut.ts` — it is complete and frozen. If a mock behavior seems wrong, record it in the SUMMARY rather than editing the client.
- Do NOT add a retry/cancel button — not in D-10 or the UI-SPEC copy contract.
  </action>

  <verify>
    <automated>npx vitest run src/lib/components/TryItOutPanel.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/components/TryItOutPanel.test.ts` passes with 0 failures, including all of T2.6-T2.11 plus every 06-01 test still green.
    - The component source contains the literal strings `Download results`, `Job failed`, `Adjust your task and try again.` and the classes `bg-red-50`, `border-red-200`, `text-red-700`, `bg-indigo-600`.
    - The Download button block is guarded by `status === 'succeeded'` and the failed block by `status === 'failed'` — grep confirms both guards exist exactly once.
    - `grep -nE "\{@html|bg-green-|text-green-" src/lib/components/TryItOutPanel.svelte` returns no matches.
    - `git diff --stat src/lib/tryItOut.ts` shows no changes.
  </acceptance_criteria>

  <done>Both demo outcomes are fully reachable and visually distinct: a task containing "fail" produces the red "Job failed" block with the verbatim error, and any other task produces the indigo "Download results" button that downloads a real text artifact naming the submitted task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the optional file input and finish the bounded auto-scrolling log box</name>

  <files>src/lib/components/TryItOutPanel.svelte, src/lib/components/TryItOutPanel.test.ts</files>

  <read_first>
    - src/lib/components/TryItOutPanel.svelte (current state after Task 1)
    - src/lib/components/FilterBar.svelte lines 19-33 (the repo's label + control markup and inline-handler convention)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-UI-SPEC.md ("UI Considerations" table rows `overflow` and `long-text` — the two backstop items this task closes; Copywriting row "File input label")
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md D-10 (auto-scrolling feed, newest at bottom) and D-13
    - src/lib/tryItOut.ts (`submitJob`'s third parameter and the `attached file:` line in the artifact text)
  </read_first>

  <behavior>
    Tests to ADD:

    - T2.12 File input renders: a `<label>` with text exactly `Attach a file (optional)` bound (via `for`/`id`) to an `<input type="file">`. The input has NO `multiple` attribute (single-file model per the assumption delta).
    - T2.13 File reaches the client END-TO-END (no module mocking): set the input's files with a `DataTransfer`-free approach —
      ```ts
      const file = new File(['a,b\n1,2\n'], 'sample.csv', { type: 'text/csv' })
      Object.defineProperty(fileInput, 'files', { value: [file], configurable: true })
      await fireEvent.change(fileInput)
      ```
      then type `'summarize the csv'`, run to `succeeded`, click `Download results` (with the `createObjectURL`/`click` stubs from T2.9) and assert `await blob.text()` contains `attached file: sample.csv`. This proves the whole chain panel → `submitJob(agentId, task, file)` → mock store → artifact, without mocking `$lib/tryItOut`.
    - T2.14 Bounded, wrapping feed (UI-SPEC `overflow` + `long-text` backstops): the feed container's `className` contains `max-h-64` and `overflow-y-auto`, and each feed line element's `className` contains `whitespace-pre-wrap` and `break-words`. Additionally assert the container is the element that `bind:this` targets by checking it also carries `font-mono`.
    - T2.15 Auto-scroll effect runs: assign a stubbed geometry to the feed element before events arrive — in jsdom `scrollHeight` is always 0, so a value assertion is meaningless. Instead assert the effect wrote to `scrollTop` by spying:
      ```ts
      const feed = container.querySelector('.font-mono') as HTMLElement
      Object.defineProperty(feed, 'scrollHeight', { value: 999, configurable: true })
      const setSpy = vi.spyOn(feed, 'scrollTop', 'set')
      await vi.advanceTimersByTimeAsync(1000); await tick()
      expect(setSpy).toHaveBeenCalledWith(999)
      ```
      If `vi.spyOn(el, 'scrollTop', 'set')` proves unusable in this jsdom version, fall back to `Object.defineProperty(feed, 'scrollTop', { set: recorder })` and assert the recorder received `999`. Record which you used.
    - T2.16 Feed ordering: newest at bottom — assert the rendered feed lines' text order matches the mock's emission order (`read data/sample.csv` before `write output/result.txt`), by mapping over the feed container's child `<p>` elements.
  </behavior>

  <action>
Insert the file input into `src/lib/components/TryItOutPanel.svelte` AFTER the task `<textarea>` and BEFORE the Run button. Add the `file` state next to the existing `task` state:

```svelte
let file = $state<File | null>(null)
```

Markup (label copy is locked by 06-UI-SPEC.md; handler style mirrors `FilterBar.svelte`'s inline handlers, not `bind:`):

```svelte
<label for="tryitout-file" class="mt-4 block text-xs font-semibold text-gray-700 mb-1">Attach a file (optional)</label>
<input
  id="tryitout-file"
  type="file"
  class="block w-full rounded border border-gray-200 bg-white px-3 py-2 text-sm text-gray-600 focus-visible:ring-2 focus-visible:ring-indigo-600"
  onchange={(e) => (file = e.currentTarget.files?.[0] ?? null)}
/>
```

Change the `submitJob` call in `run()` to pass the file through as the frozen third argument:

```svelte
const result = await submitJob(agentId, task, file)
```

Do NOT reset `file` in `run()` — the native input keeps its selection, and clearing the `$state` while the input still displays a filename would desync the two. Re-selecting replaces it (the documented single-file model).

Confirm the feed container from 06-01 already carries `max-h-64 overflow-y-auto rounded border border-gray-200 bg-gray-50 p-3 font-mono text-sm text-gray-900` and each line `py-1 whitespace-pre-wrap break-words`; add anything missing. These exact tokens satisfy 06-UI-SPEC.md's `overflow` backstop (height capped, scrolls internally) and `long-text` backstop (wraps, no horizontal overflow), and its Spacing scale (`p-3`/`py-1`).

Confirm the auto-scroll `$effect` from 06-01 is:
```svelte
$effect(() => {
  events.length
  if (feedEl) feedEl.scrollTop = feedEl.scrollHeight
})
```
Reading `events.length` is what makes the effect re-run per event; do not remove that line even though it looks like a no-op.

**PROHIBITIONS:**
- Do NOT add `multiple` to the file input.
- Do NOT read the file's contents (no `FileReader`, no `file.text()`) — threat T5. The panel only hands the `File` to `submitJob`.
- Do NOT build a "remove selected file" chip (see `<assumption_delta_decision>`).
- Do NOT switch to `bind:value` / `bind:files` — the repo convention is explicit inline handlers (06-PATTERNS.md "Inline event-handler style over bind:"). `bind:this` for the feed element is the one allowed exception (there is no handler alternative for an element ref).
- Do NOT modify `src/lib/tryItOut.ts`.
  </action>

  <verify>
    <automated>npx vitest run src/lib/components/TryItOutPanel.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/components/TryItOutPanel.test.ts` passes with 0 failures, including T2.12-T2.16 and every earlier test.
    - `grep -n "Attach a file (optional)" src/lib/components/TryItOutPanel.svelte` returns exactly one match; `grep -n "multiple" ...` returns none.
    - `grep -n "submitJob(agentId, task, file)" src/lib/components/TryItOutPanel.svelte` returns exactly one match.
    - The feed container class list contains all of `max-h-64`, `overflow-y-auto`, `font-mono`, `bg-gray-50`; feed lines contain `whitespace-pre-wrap` and `break-words`.
    - `grep -nE "FileReader|file\.text\(|readAsText" src/lib/components/TryItOutPanel.svelte` returns no matches.
    - `git diff --stat src/lib/tryItOut.ts` shows no changes.
  </acceptance_criteria>

  <done>A user can optionally attach one file whose name provably travels through the frozen client into the downloaded artifact, and the progress feed is a capped, monospace, wrapping, auto-scrolling log box that reads like a live tool-call log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove subscription cleanup — no mock timer survives unmount or a terminal job</name>

  <files>src/lib/components/TryItOutPanel.test.ts</files>

  <read_first>
    - src/lib/components/TryItOutPanel.svelte (the two `$effect` blocks and the terminal-state unsubscribe inside the `subscribeProgress` callback)
    - src/lib/tryItOut.ts (`runMockScript`'s returned unsubscribe: sets `cancelled` and `clearTimeout`s every pending timer)
    - .planning/phases/06-runnable-try-it-out-flow-mock-backed/06-CONTEXT.md D-12
    - src/lib/components/TryItOutPanel.test.ts (current suite)
    - vitest.config.ts
  </read_first>

  <behavior>
    Tests to ADD (this task is tests only — it changes no production code unless a test exposes a real leak):

    - T2.17 Unmount mid-job clears every timer (threat T2): render, type a task, click Run, `await vi.advanceTimersByTimeAsync(1000)` (job is mid-flight, feed has events), then call the `unmount()` returned by `render` → assert `vi.getTimerCount() === 0`. Then `await vi.advanceTimersByTimeAsync(10000)` and assert no error was thrown and `vi.getTimerCount()` is still `0`.
    - T2.18 No post-unmount console errors: install `const errSpy = vi.spyOn(console, 'error').mockImplementation(() => {})` before the flow in T2.17 and assert it was never called (a Svelte "state update after destroy" warning would surface here).
    - T2.19 Terminal state releases the subscription: run a job to `succeeded`, then `await vi.advanceTimersByTimeAsync(10000)` WITHOUT unmounting → the rendered feed line count is unchanged (no extra events) and status stays `succeeded` (the Run button reads `Run`, not `Running…`).
    - T2.20 Re-run does not stack subscriptions: run a job to terminal, run a second job, advance to that job's terminal → the feed contains exactly 5 lines (the success script length), not 10. Assert by counting the feed container's child `<p>` elements.

    If any of these fail, the fix belongs in `TryItOutPanel.svelte` (e.g. a missing unsubscribe call), NOT in `src/lib/tryItOut.ts` — and NOT by weakening the assertion. Record any production fix in the SUMMARY.
  </behavior>

  <action>
Add the four cleanup tests above to `src/lib/components/TryItOutPanel.test.ts`, reusing the fake-timer approach recorded in 06-01-SUMMARY.md. Capture `unmount` from the render result: `const { container, unmount } = render(TryItOutPanel, { props: { agentId: 'x' } })`.

`vi.getTimerCount()` is the load-bearing assertion — it is the only direct evidence that the mock's `clearTimeout` loop and the panel's `$effect` teardown actually ran. Do not substitute a softer proxy (like "no new DOM text") for it.

Note for the real-timer fallback path (if 06-01 recorded that fake timers did not work with `@testing-library/svelte`): `vi.getTimerCount()` is unavailable with real timers. In that case assert cleanup differently — spy on `clearTimeout` (`const clearSpy = vi.spyOn(globalThis, 'clearTimeout')`) and assert it was called at least once during `unmount()`, AND keep T2.18/T2.19/T2.20 as written (they work with real timers via `vi.waitFor`). Record which variant you used and why.

Then run the full Phase 6 verification set (see `<verification>`).

**PROHIBITIONS:**
- Do NOT add an export to `src/lib/tryItOut.ts` to make cleanup observable (D-01: the export surface is frozen at three functions + four types). Observe cleanup through timer counts / `clearTimeout` spies only.
- Do NOT skip or `it.todo` any of these four tests — timer leaks are the single highest-severity technical risk in this phase (threat T2).
  </action>

  <verify>
    <automated>npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts src/routes/agents/detail.test.ts && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0: all four test files pass with 0 failures AND `npx vite build` completes successfully (proves the Svelte 5 runes template compiles in a production build, not just under the test transformer).
    - T2.17-T2.20 all present and passing, none skipped.
    - `grep -n "getTimerCount\|clearTimeout" src/lib/components/TryItOutPanel.test.ts` returns at least one match.
    - `grep -rnE "fetch\(|EventSource|XMLHttpRequest|\{@html" src/lib/components src/routes` returns no matches.
    - `grep -n "^export" src/lib/tryItOut.ts` still lists exactly 3 functions + 4 types.
    - `git diff --name-only` lists nothing under `drizzle/`, `scripts/`, or `src/lib/spec/` (D-15).
  </acceptance_criteria>

  <done>Unmounting the panel mid-job provably leaves zero pending mock timers and logs no console errors; reaching a terminal state releases the subscription; and re-running never stacks subscriptions. The production build compiles.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/tryItOut.test.ts src/lib/components/TryItOutPanel.test.ts src/routes/agents/try-it-out.test.ts src/routes/agents/detail.test.ts` → 0 failures.
2. `npx vite build` → succeeds.
3. Transport discipline (SC-6, D-02) — must print nothing:
   `grep -rnE "fetch\(|EventSource|XMLHttpRequest|\{@html" src/lib/components src/routes`
4. Frozen export surface (D-01) — exactly 3 functions + 4 types:
   `grep -n "^export" src/lib/tryItOut.ts`
5. Client untouched by this plan: `git diff --stat src/lib/tryItOut.ts` → empty.
6. Forbidden files untouched (D-15) — must print nothing:
   `git diff --name-only | grep -E "^(drizzle/|scripts/|src/lib/spec/)"`

Known, expected, out-of-scope failure: the whole-suite `npx vitest run` still fails in `scripts/ingest.test.ts` (Phase 5 regression owned by the unexecuted plan 05-02, documented in `.planning/phases/05-*/deferred-items.md`). Do not fix it here — `scripts/` is outside this phase's file scope per D-15.
</verification>

<success_criteria>
- `TryItOutPanel.svelte` renders every element D-10 requires: task textarea, optional file input, Run button, live auto-scrolling progress feed, "Download results" on success, red failed block showing the error (SC-3, complete).
- A task containing "fail" reaches the red failed state; any other task reaches the download-enabled success state — both demoable with no code changes (SC-4, complete).
- The panel still imports nothing but the three frozen functions; no transport detail or `{@html}` anywhere in `src/lib/components` or `src/routes` (SC-6).
- No timer survives unmount or a terminal job (threat T2 closed, D-12).
- Both 06-UI-SPEC.md backstop items (feed overflow, long-text wrapping) are covered by class assertions and queued for visual confirmation in 06-03.
</success_criteria>

<artifacts_produced>
**Symbols/behavior this plan adds:**

`src/lib/components/TryItOutPanel.svelte` (completed):
- new `$state` variable `file: File | null`
- new call site `downloadArtifact(jobId!)` behind the `status === 'succeeded'` guard
- DOM contract additions other tests/verifiers rely on: `<input type="file" id="tryitout-file">` + its `Attach a file (optional)` label, a `<button>` reading `Download results`, a `.bg-red-50` failed block containing `Job failed`, the verbatim `JobUpdate.error`, and `Adjust your task and try again.`
- `submitJob(agentId, task, file)` — the third contract argument is now actually exercised

`src/lib/components/TryItOutPanel.test.ts` (completed): T2.6-T2.20 — fail path, download blob content, file end-to-end hand-off, overflow/wrap class assertions, auto-scroll effect, feed ordering, unmount timer cleanup, terminal release, re-run non-stacking.

**Unchanged by this plan (already frozen by 06-01):** all of `src/lib/tryItOut.ts`, `src/routes/agents/[slug]/+page.svelte`, `src/routes/agents/try-it-out.test.ts`.
**Produced by plan 06-03, NOT here:** `COVERAGE.md` and the human verification checkpoint.
</artifacts_produced>

<output>
After completion, create `.planning/phases/06-runnable-try-it-out-flow-mock-backed/06-02-SUMMARY.md`.

Record explicitly:
- Which auto-scroll observation technique worked (`spyOn(el, 'scrollTop', 'set')` vs `Object.defineProperty` recorder).
- Which cleanup-assertion variant was used (`vi.getTimerCount()` under fake timers vs `clearTimeout` spy under real timers).
- Any production fix made to `TryItOutPanel.svelte` because a cleanup test exposed a real leak.
- Confirmation that `src/lib/tryItOut.ts`, `drizzle/schema.ts`, `scripts/ingest.ts` and `src/lib/spec/*` were untouched.
- A one-line note that the two UI-SPEC backstop items (overflow, long-text) still need visual confirmation in 06-03.
</output>
</content>
