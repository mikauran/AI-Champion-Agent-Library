---
phase: 05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/schema.ts
  - src/routes/agents/[slug]/+page.svelte
  - src/routes/agents/try-it-out.test.ts
autonomous: true
requirements: []            # No REQUIREMENTS.md IDs assigned to Phase 5 — scope is defined by 05-CONTEXT.md
context_decisions: [D-01, D-02, D-03, D-04, D-05, D-08, D-09, D-10, D-11, D-12, D-13]
tracer: true

must_haves:
  truths:
    - "The agents table in db/catalog.db has try_it_out_mode (default 'none'), try_it_out_url, and try_it_out_task_template columns"
    - "A user visiting /agents/rfi-triage-assistant sees a 'Try it out' link that opens the placeholder URL in a new tab"
    - "A user visiting an agent detail page whose try_it_out_mode is 'none' or null sees no Try it out link and no Try it out button"
    - "A user visiting an agent detail page whose try_it_out_mode is 'runnable' sees a visually distinct disabled 'Try it out' button that does nothing when clicked"
    - "No execution/runtime code exists anywhere for runnable mode (no click handler, no fetch, no form action, no server endpoint)"
    - "src/lib/spec/types.ts, src/lib/spec/oracle-agentspec.ts and src/lib/spec/index.ts are byte-identical to their pre-phase state"
  artifacts:
    - path: "drizzle/schema.ts"
      provides: "tryItOutMode / tryItOutUrl / tryItOutTaskTemplate columns on the agents table"
      contains: "try_it_out_mode"
    - path: "src/routes/agents/[slug]/+page.svelte"
      provides: "Mode-conditional Try It Out affordance in the executive-summary area next to the GitHub link"
      contains: "tryItOutMode"
    - path: "src/routes/agents/try-it-out.test.ts"
      provides: "jsdom render tests covering all three modes (none/null, external, runnable)"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "drizzle/schema.ts"
      to: "db/catalog.db"
      via: "npx drizzle-kit push --force (ALTER TABLE agents ADD COLUMN)"
      pattern: "try_it_out_mode"
    - from: "src/routes/agents/[slug]/+page.server.ts (unchanged db.select() over all columns)"
      to: "src/routes/agents/[slug]/+page.svelte"
      via: "spread of the selected row into data.agent — new columns flow automatically"
      pattern: "agent\\.tryItOutMode"
    - from: "src/routes/agents/[slug]/+page.svelte external branch"
      to: "agent.tryItOutUrl"
      via: "href={agent.tryItOutUrl} target=\"_blank\" rel=\"noopener noreferrer\""
      pattern: "rel=\"noopener noreferrer\""
---

<objective>
Deliver the complete user-visible Try It Out affordance end-to-end: storage columns on the `agents` table, the mode-conditional affordance on the agent detail page, and `rfi-triage-assistant` set to `external` mode with a placeholder URL as a live working example.

Purpose: A visitor to an agent detail page can act on a "Try it out" affordance when one exists, and sees nothing when it doesn't. `runnable` reserves the UI slot for a future phase with zero runtime.
Output: Three new flat columns in `drizzle/schema.ts` (applied to `db/catalog.db`), a conditional Try It Out block in `+page.svelte`, a three-mode jsdom test file, and one manually-set `external` row in the dev DB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-CONTEXT.md
@.planning/ROADMAP.md

@drizzle/schema.ts
@src/routes/agents/[slug]/+page.svelte
@src/routes/agents/[slug]/+page.server.ts
@src/lib/components/TechAccordion.test.ts
</context>

<scope_lock>
**Hard file-scope lock (05-CONTEXT.md D-04 / D-05). Violating this fails the plan.**

Allowed to change in this plan:
- `drizzle/schema.ts`
- `src/routes/agents/[slug]/+page.svelte`
- `src/routes/agents/try-it-out.test.ts` (new test file — the automated proof for the UI)
- `db/catalog.db` (dev DB state only; gitignored)

FORBIDDEN — do not open with intent to edit, do not "improve while you're there":
- `src/lib/spec/types.ts` (`AgentRecord`)
- `src/lib/spec/oracle-agentspec.ts`
- `src/lib/spec/index.ts`
- `data/agents/*.yaml` (the example agent is set via a DB `UPDATE`, never via YAML — D-13)
- `src/routes/agents/[slug]/+page.server.ts` (verify no change is needed; it already selects all columns)
- `src/lib/components/TechAccordion.svelte`, `src/lib/components/CustomizationPanel.svelte` (D-12: the affordance is a primary action, not a technical detail or customization option)

Also forbidden anywhere in this phase: any runtime execution behavior for `runnable` mode — no `onclick`, no `on:click`, no `fetch`, no form action, no new server route (D-11, ROADMAP success criterion 5).
</scope_lock>

<interfaces>
Current `agents` table columns (from `drizzle/schema.ts`) — the flat-nullable convention to follow, exemplified by the `llm_*` columns:

```ts
llmTemperature: real('llm_temperature'),
llmMaxTokens:   integer('llm_max_tokens'),
llmTopP:        real('llm_top_p'),
category:       text('category'),
githubUrl:      text('github_url'),
maturityStatus: text('maturity_status').notNull().default('experimental'),
```

`AgentRow = typeof agents.$inferSelect` / `NewAgentRow = typeof agents.$inferInsert` are exported from the same file; adding columns automatically widens both types, and therefore widens `PageData.agent` in the detail route (which spreads the selected row).

Existing external-link pattern in `src/routes/agents/[slug]/+page.svelte` (lines 50-59) — mirror it exactly for `external` mode:

```svelte
{#if agent.githubUrl}
  <a href={agent.githubUrl} target="_blank" rel="noopener noreferrer"
     class="inline-flex items-center gap-2 text-sm font-medium text-indigo-600 hover:underline">
    View on GitHub &rarr;
  </a>
{/if}
```

Environment facts verified during planning (do not re-litigate):
- There is NO `sqlite3` CLI on this machine. Use `node -e` with `better-sqlite3` (already a dependency) for all DB reads/writes.
- `npx drizzle-kit push --force` runs non-interactively and reports "No changes detected" when the schema matches. `npm run dev` and `npm run build` both run `db:push` then `ingest`.
- Component tests declare their environment with a `// @vitest-environment jsdom` docblock on line 1 (see `src/lib/components/TechAccordion.test.ts`).
- Rendering the route component directly in a jsdom test works: `render(Page, { props: { data: { agent } } })` where `Page` is imported from `./[slug]/+page.svelte`. This was verified during planning against the real config (`vitest.config.ts` loads `sveltekit()` + `svelteTesting()`).
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end external mode — schema columns, applied migration, live example agent, working link</name>
  <files>drizzle/schema.ts, src/routes/agents/[slug]/+page.svelte, src/routes/agents/try-it-out.test.ts</files>
  <read_first>
    - .planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-CONTEXT.md (D-02, D-04, D-05, D-08, D-10, D-12, D-13)
    - drizzle/schema.ts (all of it — copy the flat `llm_*` / `githubUrl` column style, including the aligned-colon formatting)
    - src/routes/agents/[slug]/+page.svelte (lines 20-70 — the executive-summary section and the GitHub link block being wrapped)
    - src/routes/agents/[slug]/+page.server.ts (confirm `db.select()` selects all columns, so no change is needed here)
    - src/lib/components/TechAccordion.test.ts (lines 1-20 — the jsdom docblock + @testing-library/svelte render pattern to copy)
  </read_first>
  <action>
    Three moves, in this order.

    (a) `drizzle/schema.ts` — add three columns to the `agents` table, placed after `specId` and before `lastIngestedAt`, using the same aligned formatting as the existing columns:
    `tryItOutMode` → `text('try_it_out_mode').notNull().default('none')`;
    `tryItOutUrl` → `text('try_it_out_url')` (nullable);
    `tryItOutTaskTemplate` → `text('try_it_out_task_template')` (nullable).
    Add a short trailing comment on `tryItOutMode` noting the allowed values `none | external | runnable`. Do NOT add a JSON blob column, do NOT add a Zod/TS union type, do NOT add an enum — flat text columns only (D-08). Then apply the schema to the dev DB with `npx drizzle-kit push --force` and confirm the three columns exist with `node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db',{readonly:true});console.log(d.prepare('PRAGMA table_info(agents)').all().map(c=>c.name+':'+c.dflt_value).join('\n'))"`.

    (b) Set the example agent (D-13) with a ONE-OFF `UPDATE` against the dev DB — no new committed file, no YAML edit, no new npm script. Run exactly:
    `node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db');d.prepare(\"UPDATE agents SET try_it_out_mode='external', try_it_out_url='https://example.com/try/rfi-triage-assistant' WHERE slug='rfi-triage-assistant'\").run();d.close()"`
    Record this exact command verbatim in the plan SUMMARY under a "Re-apply after DB reset" heading — `db/` is gitignored, so the SUMMARY is the only durable record of it.

    (c) `src/routes/agents/[slug]/+page.svelte` — render the external affordance in the executive-summary `<section>` (D-12), immediately where the GitHub link lives. Wrap the existing `{#if agent.githubUrl}` block and the new Try It Out block together in a single `<div class="flex flex-wrap items-center gap-4">` so both actions sit on one row; do not alter the GitHub `<a>`'s attributes or classes. The new block guards on BOTH mode and url (url is required for external per D-02, and is nullable in the DB): condition `agent.tryItOutMode === 'external' && agent.tryItOutUrl`, rendering an `<a>` with `href={agent.tryItOutUrl}`, `target="_blank"`, `rel="noopener noreferrer"`, the same class list as the GitHub link, and the visible text `Try it out` (keep the `&rarr;` arrow convention). Only the `external` branch in this task — the `runnable` branch is Task 2. `try_it_out_task_template` is stored but NOT rendered anywhere in this phase (no decision authorizes rendering it).

    Then create `src/routes/agents/try-it-out.test.ts` with `// @vitest-environment jsdom` as line 1, importing `render` from `@testing-library/svelte` and `Page` from `./[slug]/+page.svelte`. Define a `baseAgent` fixture with every column the page and `TechAccordion` read (`slug, title, summary, systemPrompt, llmName, llmTemperature, llmMaxTokens, llmTopP, toolNames: [], requiresHumanApproval, category, githubUrl, maturityStatus, tags: [], specId, lastIngestedAt`, plus `tryItOutMode: 'none', tryItOutUrl: null, tryItOutTaskTemplate: null`) and render via `render(Page, { props: { data: { agent: { ...baseAgent, ...overrides } } } })`. In this task, add the external-mode test only: mode `'external'` with url `https://example.com/try/x` renders exactly one anchor whose text contains `Try it out`, whose `href` equals that url, whose `target` is `_blank` and whose `rel` is `noopener noreferrer`.
  </action>
  <verify>
    <automated>npx vitest run src/routes/agents/try-it-out.test.ts</automated>
    <automated>node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db',{readonly:true});const r=d.prepare(\"SELECT try_it_out_mode,try_it_out_url,try_it_out_task_template FROM agents WHERE slug='rfi-triage-assistant'\").get();console.log(JSON.stringify(r));if(r.try_it_out_mode!=='external'||!r.try_it_out_url)process.exit(1)"</automated>
    <automated>git diff --stat -- src/lib/spec data/agents src/routes/agents/\[slug\]/+page.server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/schema.ts` contains `tryItOutMode: text('try_it_out_mode').notNull().default('none')`, `tryItOutUrl: text('try_it_out_url')` and `tryItOutTaskTemplate: text('try_it_out_task_template')`, and contains no JSON-blob column and no enum/union type for the mode.
    - `PRAGMA table_info(agents)` on `db/catalog.db` lists `try_it_out_mode` (default `'none'`), `try_it_out_url`, `try_it_out_task_template`.
    - The DB read verify command prints `{"try_it_out_mode":"external","try_it_out_url":"https://example.com/try/rfi-triage-assistant","try_it_out_task_template":null}` and exits 0.
    - `src/routes/agents/[slug]/+page.svelte` contains a branch conditioned on `agent.tryItOutMode === 'external' && agent.tryItOutUrl` that renders an `<a href={agent.tryItOutUrl} target="_blank" rel="noopener noreferrer">` with visible text `Try it out`.
    - The existing GitHub `<a>` in `+page.svelte` still has its original `href`/`target`/`rel`/`class` attributes (only its surrounding wrapper changed).
    - `src/routes/agents/try-it-out.test.ts` line 1 is `// @vitest-environment jsdom`; `npx vitest run src/routes/agents/try-it-out.test.ts` passes with at least one external-mode assertion.
    - `git diff --stat -- src/lib/spec data/agents src/routes/agents/[slug]/+page.server.ts` prints no changed files (scope lock D-04/D-05 held).
    - No new file was added to the repo other than `src/routes/agents/try-it-out.test.ts` (`git status --short` shows no new script/migration file for the one-off UPDATE).
  </acceptance_criteria>
  <done>Loading /agents/rfi-triage-assistant renders a real "Try it out" link to the placeholder URL that opens in a new tab; the three columns exist in schema and DB; src/lib/spec/* and data/agents/* are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remaining modes — disabled runnable button, silent none/missing, full three-mode test coverage</name>
  <files>src/routes/agents/[slug]/+page.svelte, src/routes/agents/try-it-out.test.ts</files>
  <read_first>
    - .planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-CONTEXT.md (D-09, D-10, D-11, and the "Claude's Discretion" note on disabled styling)
    - src/routes/agents/[slug]/+page.svelte (the external branch added in Task 1 — extend it, do not duplicate it)
    - src/routes/agents/try-it-out.test.ts (the fixture and render helper from Task 1 — reuse them)
  </read_first>
  <behavior>
    - `tryItOutMode: 'external'` + url → one anchor, text contains "Try it out", href = url, target `_blank`, rel `noopener noreferrer` (already covered in Task 1; must still pass).
    - `tryItOutMode: 'runnable'` → one `<button>` with the `disabled` attribute, text contains "Try it out", and NO anchor whose text contains "Try it out".
    - `tryItOutMode: 'none'` → no element (anchor or button) whose text contains "Try it out"; the GitHub link is still rendered.
    - `tryItOutMode: null` (missing/legacy row) → same as `'none'`: nothing rendered, no crash.
    - `tryItOutMode: 'external'` but `tryItOutUrl: null` (malformed row) → nothing rendered, no anchor with an empty/undefined href.
  </behavior>
  <action>
    Extend the Task 1 conditional in `src/routes/agents/[slug]/+page.svelte` into a single `{#if} / {:else if} / {/if}` chain — no `{:else}` fallback that renders anything: the external branch stays as-is; add `{:else if agent.tryItOutMode === 'runnable'}` rendering a `<button type="button" disabled>` with text `Try it out`, styled to read clearly as non-interactive and visually distinct from the enabled link (muted gray text/background plus `cursor-not-allowed`, e.g. `inline-flex items-center gap-2 text-sm font-medium text-gray-400 bg-gray-100 px-3 py-1.5 rounded cursor-not-allowed`). Add a `title` attribute explaining it is not yet available if you want, but attach NO event handler of any kind and add NO `fetch`, no `enhance`, no form, no server endpoint (D-11). Because the chain has no other branches, `none`, `null` and malformed `external`-without-url rows render nothing automatically (D-09) — verify that by test, not by adding extra markup.

    Then add the remaining cases from `<behavior>` to `src/routes/agents/try-it-out.test.ts` as separate `it(...)` blocks reusing the Task 1 fixture. For "nothing rendered" cases, assert on the absence of a Try-it-out-labelled element specifically (e.g. query all `a` and `button` elements and assert none has `textContent` matching `/try it out/i`) rather than counting total anchors, so the unrelated "Back to catalog" and "View on GitHub" links do not make the assertion vacuous. For the runnable case assert `button.disabled === true`.

    Finally run the whole suite (`npx vitest run`) and confirm the pre-existing 57 tests still pass alongside the new ones.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <automated>grep -nE "on:click|onclick|fetch\(|use:enhance" src/routes/agents/\[slug\]/+page.svelte; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/agents/[slug]/+page.svelte` contains `{:else if agent.tryItOutMode === 'runnable'}` and a `<button` carrying both `disabled` and `cursor-not-allowed`.
    - `+page.svelte` contains no `on:click`, no `onclick`, no `fetch(`, no `use:enhance` (the grep verify exits 1 = no matches).
    - `src/routes/agents/try-it-out.test.ts` has at least 5 `it(` blocks covering external, runnable, none, null-mode, and external-without-url.
    - `npx vitest run` reports 0 failures and a total test count of at least 62 (57 pre-existing + the new ones).
    - No `{:else}` branch renders a placeholder/"coming soon" text block for `none`/null.
    - `git diff --stat -- src/lib/spec data/agents` still prints no changed files.
  </acceptance_criteria>
  <done>All three modes behave per D-09/D-10/D-11, proven by the jsdom test file; no runtime execution code exists for runnable; full suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — full suite green, including the new `src/routes/agents/try-it-out.test.ts`.
2. `npx drizzle-kit push --force` — reports "No changes detected" (schema and DB agree).
3. `node -e "const D=require('better-sqlite3');const d=new D('db/catalog.db',{readonly:true});console.table(d.prepare('SELECT slug,try_it_out_mode,try_it_out_url FROM agents').all())"` — `rfi-triage-assistant` is `external` with the placeholder URL; every other agent is `none` with a null URL.
4. `git status --short` — the only source changes are `drizzle/schema.ts`, `src/routes/agents/[slug]/+page.svelte`, and the new `src/routes/agents/try-it-out.test.ts`.
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 1 met: `drizzle/schema.ts` captures mode, url and task_template.
- ROADMAP Phase 5 criterion 3 met: detail page renders nothing / a working link / a disabled button for none-or-missing / external / runnable.
- ROADMAP Phase 5 criterion 4 met: `rfi-triage-assistant` is `external` with a placeholder URL visible on its detail page.
- ROADMAP Phase 5 criterion 5 met: zero runtime execution behavior added for `runnable`.
- Scope lock held: no changes under `src/lib/spec/`, `data/agents/`, or `+page.server.ts`.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**Drizzle schema (`drizzle/schema.ts`, this plan) — new columns on `agents`:**
- `tryItOutMode` → `try_it_out_mode` TEXT NOT NULL DEFAULT `'none'` (allowed values: `none` | `external` | `runnable`)
- `tryItOutUrl` → `try_it_out_url` TEXT NULL (link target for `external`)
- `tryItOutTaskTemplate` → `try_it_out_task_template` TEXT NULL (stored only; not rendered this phase)
- Widened (no new declarations): `AgentRow` / `NewAgentRow` inferred types, and transitively `PageData.agent` in the agent detail route

**No new TypeScript types, no new exports, no new Zod schemas, no new components, no new routes, no new npm scripts.**

**UI (`src/routes/agents/[slug]/+page.svelte`, this plan):**
- New `<div class="flex flex-wrap items-center gap-4">` wrapper around the GitHub link + Try It Out affordance in the executive-summary section
- New conditional chain: `{#if agent.tryItOutMode === 'external' && agent.tryItOutUrl}` → `<a target="_blank" rel="noopener noreferrer">Try it out</a>`; `{:else if agent.tryItOutMode === 'runnable'}` → disabled `<button>Try it out</button>`; no other branch (none/null/malformed render nothing)

**Tests (this plan):**
- `src/routes/agents/try-it-out.test.ts` (new) — jsdom render tests for external / runnable / none / null-mode / external-without-url

**Dev DB state (gitignored, this plan):**
- `db/catalog.db` row `rfi-triage-assistant`: `try_it_out_mode='external'`, `try_it_out_url='https://example.com/try/rfi-triage-assistant'` — set by a one-off `node -e` UPDATE recorded verbatim in `05-01-SUMMARY.md`, not by any committed script or YAML edit

**Produced by plan 05-02 (not here):** `flattenRecord()` try_it_out literal defaults and the documented `onConflictDoUpdate` omission in `scripts/ingest.ts`; `CREATE_TABLE_SQL` + two regression tests in `scripts/ingest.test.ts`.

**Explicitly NOT produced (deferred):** any `runnable` execution runtime; `try_it_out` in the Oracle AgentSpec YAML / `AgentRecord` pipeline; `try_it_out` on the catalog or search surfaces; any rendering of `task_template`.
</artifacts_produced>

<output>
After completion, create `.planning/phases/05-add-an-optional-try-it-out-field-to-the-agent-catalog-shape/05-01-SUMMARY.md`, including the verbatim one-off `UPDATE` command under a "Re-apply after DB reset" heading (db/ is gitignored).
</output>
